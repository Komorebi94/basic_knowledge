import type { Article } from './types'

export const javascriptArticles: Article[] = [
    {
        slug: 'event-loop-2026',
        module: 'javascript',
        title: '事件循环：任务、微任务与渲染时机',
        summary: '宏任务、微任务、requestAnimationFrame 和调度优先级，决定异步代码何时真正执行。',
        depth: 'core',
        heat: 5,
        year: 2026,
        tags: ['event loop', 'microtask', '调度'],
        overview:
            'JS 在一个线程上跑。事件循环每次取出一个任务，清空微任务队列，必要时渲染。Promise、queueMicrotask、MutationObserver 都走微任务。',
        body: `## 1. 尽可能详细地理解问题

JavaScript 的“单线程”是指同一时刻只有一个调用栈在执行 JavaScript，但浏览器并不只有一个线程。计时器、网络请求、用户输入和渲染由宿主环境处理；当对应工作可以继续时，宿主把回调放入队列，事件循环负责选择合适的时机让它进入调用栈。

一次典型循环可以拆成以下过程：

1. 从任务队列取出一个任务，例如整段 script、点击回调或计时器回调。
2. 从头到尾执行这个任务，期间注册的异步回调不会打断当前调用栈。
3. 当前栈清空后，持续清空微任务队列；微任务创建的新微任务也要在这一轮继续执行。
4. 浏览器获得渲染机会，判断是否需要进行样式计算、布局、绘制和合成。
5. 开始下一轮，继续取任务。

Promise 的 \`then/catch/finally\`、\`queueMicrotask\` 以及 \`await\` 后续代码通常进入微任务队列；\`setTimeout\`、消息事件和用户交互回调属于任务。因而同步代码一定先完成，随后执行本轮微任务，最后才轮到下一个任务。

例如先注册 \`setTimeout(fn, 0)\`，再执行 \`Promise.resolve().then(fn2)\`，通常是 \`fn2\` 先运行。这里的 0 只表示达到最短等待时间后可以排队，不表示立即执行。计时器仍需等待当前任务、全部微任务以及调度条件。

事件循环还直接影响页面流畅度。一个任务如果连续计算数百毫秒，调用栈一直不空，浏览器便不能及时响应输入或绘制。把大计算拆成多个任务可以给渲染让出机会；但拆成大量微任务通常无效，因为浏览器会先清空微任务，仍可能造成“微任务饥饿”。纯计算量很大时，应考虑 Web Worker，而不仅是换一种异步写法。

Node.js 同样有任务与微任务，但它还有 timers、poll、check 等阶段，\`setImmediate\` 与 \`setTimeout\` 的先后会受所处阶段影响。因此可以共享“调用栈清空后处理微任务”的核心认识，但不能把浏览器每个队列的细节原样套到 Node.js。

## 2. 总结回答话术

> JavaScript 本身用单一调用栈执行代码，浏览器把计时器、网络和事件等工作交给宿主能力处理。事件循环每次执行一个任务，调用栈清空后会把当前微任务队列全部清空，然后浏览器才可能渲染，再进入下一个任务。Promise 回调和 await 后续属于微任务，setTimeout 回调属于任务，所以微任务通常更早执行。需要注意，长任务会阻塞交互和渲染，而不断产生的微任务也可能让渲染一直得不到机会；大任务应分片，重计算则适合放到 Worker。

## 3. 可拓展的问题

- **为什么 \`await 1\` 也会产生异步边界？** \`await\` 会把后续执行安排为微任务，即使等待的不是 Promise。
- **\`requestAnimationFrame\` 在什么时机执行？** 它在浏览器准备下一帧绘制前执行，适合更新动画状态，但不是普通任务的简单替代品。
- **如何判断并治理长任务？** 可以结合 Performance 面板、Long Tasks API 和交互延迟定位，再选择分片、延后或 Worker。
- **微任务为什么可能饿死渲染？** 清空微任务是一个持续过程；微任务若不断追加微任务，浏览器就无法进入渲染机会。
- **Node.js 中 \`process.nextTick\` 有什么特殊性？** 它有独立且更高优先级的队列，滥用同样可能阻塞其他阶段。`,
        pitfalls: [
            '在微任务死循环里不断 then，渲染永远插不进去。',
            '用 setTimeout 模拟 next paint，实际可能早于或晚于绘制。',
            '把 async 函数当成并行线程，内部仍是单线程交错。',
        ],
    },
    {
        slug: 'es2024-collections',
        module: 'javascript',
        title: 'ES2024 集合方法与 Object.groupBy',
        summary: 'Set 补齐数学运算，groupBy 成为分组的标准库写法。',
        depth: 'core',
        heat: 2,
        year: 2026,
        tags: ['ES2024', 'Set', 'groupBy'],
        overview:
            'ES2024 给 Set 增加了 `union` / `intersection` / `difference` / `symmetricDifference` / `isSubsetOf` 等，`Object.groupBy` 与 `Map.groupBy` 进入语言。',
        body: `## 1. 尽可能详细地理解问题

\`Set\` 表达的是“不重复值的集合”。当业务语义是权限集合、标签集合、已选择的 ID 或功能开关时，使用集合运算比嵌套数组循环更贴近问题本身，也能避免重复值影响结果。

常见运算可以这样理解：

- \`a.union(b)\` 是并集，结果包含 a 或 b 中出现的所有值。
- \`a.intersection(b)\` 是交集，只保留两个集合共同拥有的值。
- \`a.difference(b)\` 是差集，只保留 a 中存在而 b 中不存在的值；它有方向性。
- \`a.symmetricDifference(b)\` 是对称差集，只保留恰好出现在其中一个集合的值。
- \`a.isSubsetOf(b)\` 判断 a 是否为 b 的子集。
- \`a.isDisjointFrom(b)\` 判断两个集合是否完全没有交集。

这些方法返回新集合，不修改原集合。这一点有利于状态管理和引用比较：旧状态仍保持不变，新结果拥有新的引用。集合判断采用 SameValueZero 规则，\`NaN\` 能与自身匹配，\`0\` 和 \`-0\` 被视为相同；对象则仍按引用判断，内容相同的两个对象不是同一个集合成员。

\`Object.groupBy(items, selector)\` 解决的是“按照某个分类键把元素分组”。选择函数返回的键会转换为属性键，结果是一个无原型对象，每个属性对应一个数组。如果分类键本身是对象，或需要保持键的真实类型，应该使用 \`Map.groupBy\`，因为 Map 按值或对象引用保存键，不会统一字符串化。

分组和聚合不是一回事。groupBy 只负责把数据放进不同桶中，不会自动求和、排序或生成统计结果。若需求是按部门分组后计算总工资，可以先 groupBy 再遍历聚合；如果只需一次遍历且规则高度定制，\`reduce\` 依然清晰有效。

还要考虑运行环境。语言提供了新方法，不代表项目支持的每个浏览器或 Node.js 版本都已实现。应根据目标环境配置转译或 polyfill，并避免只在开发机验证后直接上线。

## 2. 总结回答话术

> Set 适合表达唯一值集合，新的集合方法把并集、交集、差集、子集等常见逻辑标准化，而且通常返回新 Set，不修改原集合。Object.groupBy 和 Map.groupBy 用来按规则分组：前者适合字符串或 Symbol 属性键，后者适合对象等任意类型的键。它们只做分组，不负责求和或排序，复杂聚合仍可以使用 reduce。实际使用时还需要注意对象按引用去重，以及目标运行环境是否支持这些方法。

## 3. 可拓展的问题

- **Set 的查找为什么通常比数组 \`includes\` 更合适？** Set 为成员判断设计，平均查找成本通常更低，但小数据量仍应优先考虑可读性。
- **两个内容相同的对象能在 Set 中去重吗？** 不能自动按内容去重；需要先选取稳定键，或自行定义结构相等规则。
- **\`Object.groupBy\` 的结果为什么没有普通对象原型？** 这样可以减少分类键与继承属性冲突，但调用对象原型方法时要使用安全写法。
- **分组后怎样保持排序？** 分组保持元素的遍历顺序，但组之间的展示顺序应由业务显式排序，必要时选 Map。
- **什么时候不该使用 Set？** 需要保留重复次数、按索引随机访问或表达有序重复序列时，数组通常更合适。`,
        pitfalls: [
            'Object.groupBy 的键一律字符串化，对象当 key 会变成 [object Object]。',
            '把 Set 方法当成 in-place，继续用旧引用做全等判断。',
            '在不支持的运行时直接用，缺少构建期 polyfill。',
        ],
    },
    {
        slug: 'iterator-helpers',
        module: 'javascript',
        title: 'Iterator Helpers：惰性序列操作',
        summary: 'map/filter/take/drop 可以直接作用在迭代器上，避免中间数组。',
        depth: 'core',
        heat: 2,
        year: 2026,
        tags: ['iterator', 'ES2025', '惰性'],
        overview:
            'Iterator Helpers 让 `iterator.map().filter().take(n)` 惰性执行。适合大数据流、分页游标、生成器管道。',
        body: `## 1. 尽可能详细地理解问题

先区分 iterable 和 iterator。iterable 是“可以创建迭代器的对象”，它实现 \`Symbol.iterator\`；iterator 是“记录当前遍历位置的游标”，它提供 \`next()\`，每次返回 \`{ value, done }\`。数组通常是可重复遍历的 iterable，而从数组取得的 iterator 通常只能向前消费一次。

数组的 \`map\`、\`filter\` 会立刻遍历全部元素，并为每一步创建完整的中间数组。例如一百万条数据先 filter 再 map，即使最后只取前 10 条，也可能先处理完整个集合。Iterator Helpers 把这些操作组合成惰性管道：只有消费者调用 \`next()\` 时，数据才逐个经过 filter 和 map。

例如 \`tasks.values().filter(isDone).map(getId).take(10)\` 的目标不是立刻得到数组，而是得到一个新的迭代器。消费它时，流程会一直读取源数据，直到找到 10 个满足条件的 ID，然后停止读取后续内容。这既减少中间数组，也能避免无用计算。

惰性特别适合以下场景：

- 输入规模很大，但只需要前一部分结果。
- 数据由生成器逐步产生，甚至是理论上的无限序列。
- 需要串联多个转换，又不希望每一步都物化数组。
- 从 Map、Set 等结构取得的迭代器可以直接进入处理管道。

代价是迭代器通常具有状态且只能消费一次。第一次调用 \`next()\` 后游标已经前移；遍历完成后再次遍历只能得到空结果。如果页面渲染、排序、按索引读取或重复计算都需要完整数据，应在合适的边界调用 \`toArray()\`，或者让数据源成为每次都能创建新 iterator 的 iterable。

同步 iterator 也不能直接处理需要等待的转换。回调返回 Promise 不会让同步管道自动逐个 await；流式网络数据等异步来源需要使用异步迭代协议和对应能力，不能只看方法名相似就混用。

## 2. 总结回答话术

> Iterator Helpers 给迭代器提供 map、filter、take 等链式操作，核心价值是惰性计算。数组方法会立即遍历并创建中间数组，而迭代器只有在 next 或最终消费时才处理一个值，所以对大数据、生成器和只取部分结果的场景更节省计算和内存。需要注意普通 iterator 是有状态、一次性的；如果要重复遍历、排序或随机访问，就应该在边界转成数组，或者使用可重新创建迭代器的 iterable。

## 3. 可拓展的问题

- **iterable 与 iterator 的根本区别是什么？** 前者描述可遍历能力，后者保存一次遍历过程中的游标状态。
- **惰性计算一定更快吗？** 不一定；小数组或最终必须消费全部数据时，收益可能有限，还会增加理解成本。
- **无限序列为什么必须配合 \`take\` 等终止条件？** 没有终止条件的完整消费永远不会结束。
- **迭代过程中发生异常怎么办？** 消费立即停止；生成器还可能通过 \`return()\` 执行清理逻辑。
- **什么时候应该调用 \`toArray()\`？** 当数据要进入需要随机访问、重复遍历或完整排序的系统边界时。`,
        pitfalls: [
            '对已耗尽的 iterator 再循环，得到空序列却以为是 filter 写错。',
            '在需要随机访问的 UI 列表上强行用惰性流，最后还是要 toArray。',
            '忘记同步与异步迭代器不是同一套 helper。',
        ],
    },
    {
        slug: 'promise-try',
        module: 'javascript',
        title: 'Promise.try 与统一错误通道',
        summary: '同步抛错和异步拒绝收进同一条 Promise 链。',
        depth: 'intro',
        heat: 1,
        year: 2026,
        tags: ['Promise.try', '错误处理'],
        overview:
            '`Promise.try(fn)` 执行 fn：同步 return 会 resolve，同步 throw 会 reject，返回 Promise 则直接衔接。这样校验失败和网络失败走同一 `catch`。',
        body: `## 1. 尽可能详细地理解问题

调用一个来源不确定的函数时，它可能有三种结果：直接返回普通值、同步抛出异常、返回一个稍后兑现或拒绝的 Promise。调用方若想统一使用 Promise 链处理，就需要把这三种结果放进同一条异步通道。

直接写 \`Promise.resolve(fn())\` 并不能捕获 \`fn()\` 的同步异常，因为 JavaScript 会先执行函数调用，再把返回值传给 \`Promise.resolve\`；如果调用阶段已经抛错，Promise 还没有机会接管。传统做法是写一层 \`new Promise((resolve) => resolve(fn()))\`，Promise 构造器会把执行器中的异常转换为拒绝，但表达意图不够直接。

\`Promise.try(fn, ...args)\` 的作用就是安全调用函数并返回 Promise：

- fn 返回普通值，结果 Promise 以该值兑现。
- fn 同步抛错，结果 Promise 以该错误拒绝。
- fn 返回 Promise 或 thenable，结果 Promise 会采用它的最终状态。

它适合放在边界层。例如插件钩子可能由不同开发者实现，有的同步返回配置，有的异步读缓存；统一包装后，上层只需使用 \`then/catch\` 或 \`await/try-catch\`，不必先判断返回类型。

这并不意味着所有 async 函数外都需要再包一层。async 函数本身已经会把返回值包装为 Promise，并把同步抛错转换为拒绝。Promise.try 更适合包装“契约允许同步或异步”的普通回调，而不是机械地增加包装。

它也不提供并发、超时或取消。\`Promise.allSettled\` 负责收集多个任务的结果，\`AbortSignal\` 负责向底层操作传递取消意图，超时则需要计时与取消策略。Promise.try 只解决入口调用结果不统一的问题。

错误统一后仍要分类处理。参数不合法、网络失败、用户主动取消和代码缺陷不应全部吞掉。边界层可以补充上下文，但无法处理的错误应继续抛出，让监控和上层逻辑感知。

## 2. 总结回答话术

> Promise.try 用来调用一个可能同步也可能异步的函数，并把普通返回值、同步异常和 Promise 结果统一成 Promise。它与 Promise.resolve(fn()) 的关键区别是，后者会先执行 fn，所以接不住调用时的同步异常。这个能力适合插件、适配器或回调边界；如果函数本来就是 async，通常不需要重复包装。Promise.try 也不负责并发和取消，这些仍要结合 allSettled、AbortSignal 等机制处理。

## 3. 可拓展的问题

- **为什么 \`Promise.resolve().then(fn)\` 也能捕获同步异常？** fn 在 then 的微任务中执行，异常会转成该链的拒绝，但执行时机会被推迟。
- **async 函数为何天然具备类似效果？** async 函数的返回值总会转成 Promise，函数体抛出的异常也会成为拒绝。
- **Promise 拒绝与同步异常应该完全同等处理吗？** 传播通道可以统一，但日志、恢复策略和用户提示通常仍需按错误类型区分。
- **怎样给 Promise 增加取消能力？** Promise 本身不可取消，应将 AbortSignal 传给真正执行工作的底层 API。
- **\`all\` 与 \`allSettled\` 应如何选择？** 前者适合任一失败就无法继续的整体任务，后者适合需要收集每项结果的批处理。`,
        pitfalls: [
            '在已经是 async 的函数外包一层无意义的 try，只增加缩进。',
            '把业务错误和编程错误都吞成空 catch。',
            '忽略取消，页面离开后仍继续写状态。',
        ],
    },
    {
        slug: 'es-modules-tla',
        module: 'javascript',
        title: 'ESM 与 Top-level await',
        summary: '模块图会等待顶层 await，加载顺序和循环依赖都受影响。',
        depth: 'core',
        heat: 4,
        year: 2026,
        tags: ['ESM', 'top-level await'],
        overview:
            'ESM 是浏览器和 Node 的默认模块格式。文件顶层 `await` 会让依赖它的模块等到这个 Promise 结束再继续求值。',
        body: `## 1. 尽可能详细地理解问题

ES Module 并不是按照源码中 import 的书写顺序简单执行文件。宿主会先解析入口，递归发现依赖，建立模块图并连接各模块的导入导出绑定，最后才按照依赖关系求值。静态 import 必须出现在模块顶层，使工具在不执行代码时就能分析依赖、检查导出并进行 tree shaking。

ESM 的导出是实时绑定，不是导入时复制一份值。如果模块 A 导出变量 count，之后更新它，导入方读取到的是新值。但绑定在声明完成前仍处于暂时性死区；循环依赖中，如果 A 求值时立即读取尚未初始化的 B 导出，就可能抛出错误。循环关系本身不一定失败，失败通常来自“过早读取”。

Top-level await 允许在模块最外层等待 Promise，不必额外包 async 函数。当模块执行到顶层 await 时，它的求值会暂停；依赖它的父模块也必须等待，但模块图中与它无依赖关系的分支仍可能继续执行。因此它形成的是模块图中的异步边界，不等同于让所有模块完全串行。

适合使用的场景通常是应用启动前必须完成的初始化，例如加载关键配置、初始化 WebAssembly 或按环境加载兼容代码。若普通工具模块在顶层发起网络请求，任何间接依赖它的入口都会被阻塞，失败也可能导致整条模块链无法启动。更可控的方式是显式导出初始化函数或 Promise，由应用入口决定何时等待，并负责超时、重试和降级。

打包器需要保留这种异步求值语义，代码分割后的 chunk 加载也可能成为等待链的一部分。库作者尤其要谨慎，因为消费方的构建目标、CommonJS 互操作方式和运行环境各不相同。CommonJS 的 \`require\` 是同步接口，无法自然表达一个仍在顶层等待的 ESM 模块，通常需要改用动态 \`import()\`。

## 2. 总结回答话术

> ESM 会先解析并连接完整的模块依赖图，再按依赖关系求值；import 得到的是导出的实时绑定。Top-level await 会把当前模块变成异步求值边界，依赖它的上游模块必须等待，但无关分支仍可继续。它适合必须在模块可用前完成的关键初始化，不过放在底层公共模块会扩大阻塞范围，也会放大循环依赖和 CommonJS 互操作问题，所以通常应把异步初始化边界放在应用入口并显式处理失败。

## 3. 可拓展的问题

- **ESM 的实时绑定与 CommonJS 导出对象有什么区别？** ESM 由语言维护绑定关系，CommonJS 通常暴露运行时对象，两者的加载和更新语义不同。
- **循环依赖一定会报错吗？** 不一定；关键在于模块求值期间是否读取了对方尚未初始化的绑定。
- **动态 \`import()\` 与静态 import 有什么区别？** 前者运行时执行并返回 Promise，可形成按需加载边界；后者可被静态分析。
- **顶层 await 会不会阻塞整个应用？** 它会阻塞依赖链上的模块，不相关的图分支仍可能求值，但入口通常最终需要等待关键分支。
- **如何降低初始化瀑布？** 并行发起互不依赖的操作、把非关键初始化延后，并避免底层模块隐藏网络副作用。`,
        pitfalls: [
            '在共享 utils 顶层 await，拖慢所有无关页面。',
            '循环 import + TLA，启动阶段出现难以复现的时序 bug。',
            '混用 CJS default 解构，拿到 { default: fn }。',
        ],
    },
    {
        slug: 'temporal-status',
        module: 'javascript',
        title: 'Temporal：日期时间的继任者',
        summary: '用明确的时区与日历类型替换 Date 的隐式行为，落地时要核对运行时。',
        depth: 'deep',
        heat: 2,
        year: 2026,
        tags: ['Temporal', '时区', 'Date'],
        overview:
            'Temporal 提供 `PlainDate`、`Instant`、`ZonedDateTime` 等类型，把「墙上时间」和「绝对时间」分开。`Date` 的可变性和时区陷阱正是它要消灭的。',
        body: `## 1. 尽可能详细地理解问题

日期时间最容易出错的根源，是把几种不同概念都塞进一个 Date：某个绝对瞬间、某地墙上显示的时间、只包含年月日的日历日期、时间长度以及时区规则。Temporal 用不同类型表达不同语义，让错误更早暴露。

常用类型可以这样选择：

- \`Temporal.Instant\` 表示时间线上的唯一瞬间，适合日志时间、创建时间和跨系统传输。
- \`Temporal.PlainDate\` 只表示日历日期，例如生日或账单日，本身没有时区，也不对应唯一瞬间。
- \`Temporal.PlainDateTime\` 表示日期加墙上时间，但仍没有时区，例如“当地 2026-09-03 09:00”。
- \`Temporal.ZonedDateTime\` 同时包含瞬间、时区和日历，适合航班、跨地区会议等需要时区规则的事件。
- \`Temporal.Duration\` 表示一段时间，但“一个月”具体有多少天要结合起点才能计算。

时区不是固定偏移量。像 Asia/Shanghai 这样的时区标识背后是一组历史和未来规则，而 \`+08:00\` 只是偏移。存在夏令时的地区，一天可能不是严格的 24 小时。给 ZonedDateTime 加“一天”通常保持下一天相同的墙上时间；加“24 小时”则保持时间线上的精确时长，两者跨越夏令时边界时结果可能不同。

这也决定了存储策略。后端记录已经发生的事件时，通常存 Instant 或带偏移的标准字符串；如果记录的是“每周一上午九点”这类未来规则，还必须保存目标时区，不能过早换算成一个固定 UTC 时间。生日等纯日期则不应强行附加午夜时区，否则跨地区展示可能变成前一天。

Temporal 的对象采用不可变设计，调用 add、with 等操作会返回新对象，不会悄悄修改原值。解析时也应优先使用明确格式，避免依赖不同环境对模糊日期字符串的猜测。项目落地前仍需确认目标运行环境；未原生支持时可以在日期边界引入 polyfill，避免多个模块各自混用 Date 和 Temporal 规则。

## 2. 总结回答话术

> Temporal 的核心不是提供更多日期方法，而是用不同类型区分不同时间语义：Instant 表示全局唯一瞬间，PlainDate 表示不带时区的日历日期，ZonedDateTime 表示某个时区中的具体时间，Duration 表示时长。这样能避免 Date 把本地时间、UTC 和时区转换混在一起。跨夏令时时，加一天和加 24 小时可能不同；存储已发生事件通常用 Instant，未来的本地日程则还要保留时区。Temporal 对象不可变，落地时还需确认运行环境或 polyfill。

## 3. 可拓展的问题

- **为什么生日不适合存成 UTC 午夜？** 生日是纯日历日期，附加时区后跨区转换可能造成日期前移或后移。
- **时区和 UTC 偏移有什么区别？** 偏移只是某一刻的数值，时区包含会随日期变化的完整规则。
- **“加一天”和“加 24 小时”什么时候不同？** 跨越夏令时切换时，墙上时间和绝对时长会产生差异。
- **前后端怎样约定日期字段？** 每个字段都应明确它是瞬间、纯日期还是带时区的未来时间，并使用对应序列化格式。
- **如何渐进迁移 Date 代码？** 先统一系统边界的解析、存储和展示，再逐步替换核心日期运算，避免一次性重写。`,
        pitfalls: [
            '继续用 Date 做本地日期加减，夏令时会跳一小时。',
            '把 PlainDate 当 Instant 存进后端，时区信息被丢掉。',
            'polyfill 全量引入，日期工具变成最大的 JS 包之一。',
        ],
    },
]

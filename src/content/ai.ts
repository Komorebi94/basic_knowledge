import type { Article } from './types'
import { createReviewBody } from './review-body'

export const aiArticles: Article[] = [
    {
        slug: 'llm-foundations',
        module: 'ai',
        title: 'LLM 基础：Token、Transformer 与生成',
        summary: '理解模型怎样把文本变成 token、计算上下文关系，并逐个预测下一个 token。',
        depth: 'intro',
        heat: 3,
        year: 2026,
        tags: ['LLM', 'Transformer', 'Token'],
        overview:
            '大语言模型本质上是在上下文条件下预测下一个 token 的概率分布。它能生成连贯内容，但不等于保存了一套可查询、始终正确的事实数据库。',
        body: createReviewBody(
            `## 从文字到 Token

模型不直接读取“字”或“单词”，而是先经过 tokenizer 切成 token，再映射成向量。一个中文字符可能对应一个或多个 token，代码、空格和生僻词也会影响数量。因此上下文窗口、输入成本和输出上限都按 token 计算，不能简单按字符数估算。

每个 token 先变成 embedding，并叠加位置信息。Transformer 的 self-attention 会让当前位置根据上下文中的其他位置计算权重，得到与当前语境相关的表示。多层注意力和前馈网络不断变换表示，最后输出词表上“下一个 token”的概率分布。

## 为什么每次回答可能不同

生成时会从概率分布中选择 token。temperature 越高，概率分布通常越平，结果更发散；越低越稳定，但低温也不能保证事实正确。top-p 等参数限制候选集合。模型每生成一个 token，就把它追加到上下文继续预测，直到停止标记或达到长度上限。

模型的参数学习了语言和知识模式，却没有像数据库一样保存事实来源。它优化的是“合理续写”，不是“证明为真”，所以会出现语句流畅但事实错误的幻觉。需要外部知识时，应使用检索或工具；需要固定格式时，应使用结构化输出和运行时校验。

## 训练、微调与推理

预训练通过海量数据学习通用模式；指令微调让模型更会遵循任务描述；偏好优化让输出更符合人类偏好和安全要求。真正处理用户请求的阶段叫推理。推理时系统提示、用户消息、历史、检索片段和工具结果共同构成上下文，但不会自动永久写回模型参数。

## 工程上真正关心什么

选择模型不能只看参数规模。还要比较任务正确率、上下文长度、首 token 延迟、生成速度、结构化输出与工具调用能力、价格、数据合规和稳定性。对固定任务做自己的评估集，比通用榜单更有意义。`,
            `LLM 会把输入切成 token，经 embedding 和 Transformer attention 建模上下文，再逐 token 预测后续内容。采样参数影响稳定性和多样性，但不保证正确。模型学到的是语言与知识模式，不是带来源的事实数据库，所以外部事实要用检索或工具，固定格式要用 schema 校验。预训练、指令微调和推理是不同阶段，选型最终要看真实任务的质量、延迟、成本和合规。`,
            [
                {
                    question: 'Token 与字符是什么关系？',
                    direction:
                        '不是一一对应，语言、词频、空格和代码符号都会影响切分，因此应使用对应 tokenizer 估算。',
                },
                {
                    question: 'temperature 设为 0 就不会幻觉吗？',
                    direction: '不会；它只是让选择更确定，模型仍可能稳定地输出同一个错误答案。',
                },
                {
                    question: '上下文内容会自动训练进模型吗？',
                    direction:
                        '单次推理只临时使用上下文，是否用于后续训练取决于产品的数据政策与明确设置。',
                },
                {
                    question: '为什么通用榜单不能直接决定模型选型？',
                    direction:
                        '榜单任务、语言、提示和评分方式可能与业务不同，应以自有代表性任务评估。',
                },
            ],
        ),
        pitfalls: [
            '把模型当成会精确查询事实和来源的数据库。',
            '只调高或调低 temperature，就以为解决了正确性问题。',
            '只按模型大小选型，不测真实任务的质量、延迟和成本。',
        ],
    },
    {
        slug: 'prompt-and-structured-output',
        module: 'ai',
        title: 'Prompt 设计与结构化输出',
        summary: '把任务、上下文、约束和输出合同分开，并在运行时验证模型结果。',
        depth: 'core',
        heat: 3,
        year: 2026,
        tags: ['Prompt', 'JSON Schema', '结构化输出'],
        overview:
            '稳定的 Prompt 不是堆叠话术，而是清楚描述任务和边界。机器消费的结果必须有 schema、校验、错误处理和有限重试。',
        body: createReviewBody(
            `## 一个可维护的 Prompt

把内容分成四层：角色与长期规则、当前任务、可信上下文、输出合同。长期规则只放跨请求稳定的内容；用户数据和检索文本要明确标记为“不可信内容”，不能混进系统规则。任务中说明目标、受众、禁止事项和完成标准，避免“尽量好”“专业一点”这类不可判断的描述。

Few-shot 示例适合传达格式、边界判断和少见模式，但示例必须覆盖真正困难的情况。示例太多会占窗口，也可能让模型机械模仿。要求模型输出隐藏推理过程既不可靠也没必要；生产系统更需要简短结论、证据、结构化字段和可验证步骤。

## 结构化输出不是“请返回 JSON”

如果下游程序要消费结果，应提供 JSON Schema 或等价类型约束：字段名、类型、枚举、必填项、数组上限和是否允许额外字段。收到结果后仍要运行时校验，因为模型可能漏字段、返回不存在的枚举、把数字写成字符串，或者在 JSON 外再加解释。

校验失败时，可以把简化后的错误反馈给模型做一次或少量修复；不要无限重试。重试要有上限、退避和总成本预算。业务语义也要二次校验，例如日期先后、金额范围、资源是否存在、当前用户是否有权限，这些不是 JSON Schema 能独立保证的。

## Prompt 的版本和测试

Prompt 是生产代码的一部分，应进入版本管理，记录模型、参数和模板版本。修改前用固定评估集跑基线，比较任务成功率、格式通过率、拒答率、延迟和成本。不要只挑几个成功案例肉眼判断。

Prompt 注入不能靠一句“忽略恶意指令”解决。真正的防线是数据与指令分层、最小权限工具、服务端鉴权、输出校验，以及不让模型接触任务不需要的秘密。`,
            `Prompt 应拆成长期规则、当前任务、可信或不可信上下文以及输出合同。机器消费的输出必须用 schema 约束并在运行时校验，校验失败只能有限重试，权限和业务语义还要独立检查。Prompt 需要版本管理和回归评估。提示注入不是文案问题，必须同时依赖指令分层、最小权限和服务端安全边界。`,
            [
                {
                    question: 'system prompt 是否优先级最高，所以一定安全？',
                    direction: '它能提高指令优先级，但不能代替鉴权、隔离和工具权限控制。',
                },
                {
                    question: '什么时候使用 few-shot？',
                    direction: '当规则难用文字说清、需要稳定格式或边界示范时使用，并控制示例数量。',
                },
                {
                    question: 'JSON Schema 能保证业务正确吗？',
                    direction: '只能保证结构，资源存在性、权限、金额和日期关系仍需业务校验。',
                },
                {
                    question: '怎样判断 Prompt 修改是否变好？',
                    direction: '在固定评估集上与基线比较质量、格式、延迟和成本，不能只看个别样例。',
                },
            ],
        ),
        pitfalls: [
            '把用户输入直接拼进系统规则，导致指令和数据边界消失。',
            '只写“返回 JSON”，却没有 schema 和运行时校验。',
            '改 Prompt 后只看几个成功案例，没有回归评估。',
        ],
    },
    {
        slug: 'embeddings-vector-search',
        module: 'ai',
        title: 'Embedding 与向量检索',
        summary: '把文本映射到语义向量，用相似度召回相关内容，再结合关键词与元数据过滤。',
        depth: 'core',
        heat: 3,
        year: 2026,
        tags: ['Embedding', '向量数据库', '相似度'],
        overview:
            'Embedding 将文本、图片等对象编码为向量，语义相近的内容在空间中通常更接近。向量检索负责候选召回，不等同于最终答案。',
        body: createReviewBody(
            `## Embedding 做了什么

Embedding 模型把输入编码成固定维度的浮点向量。检索时把查询也编码成同一向量空间，再用余弦相似度、点积或欧氏距离寻找近邻。距离指标必须与模型和索引配置一致，不能随意混用；部分模型还要求归一化。

向量能找到“意思相近但用词不同”的内容，例如“怎么退款”和“撤销订单并退回款项”。它不擅长所有精确匹配：订单号、错误码、人名和缩写往往更适合关键词检索。因此生产系统常用向量 + BM25 + 元数据过滤，再进行重排。

## 索引和数据生命周期

写入前要清洗、切片并保留文档 id、版本、标题、权限、时间和来源位置。更新文档时必须删除或失效旧向量，否则新旧规则会同时被召回。Embedding 模型或维度变化通常需要重建索引，并在迁移期进行版本隔离。

近似最近邻索引用速度和内存换精度。数据量小时可以精确搜索，规模扩大后再选 HNSW、IVF 等索引策略。参数要根据召回率、查询延迟、写入频率和内存实测，不应照抄默认值。

## 如何评估检索

建立“查询—相关文档”标注集，关注 Recall@K、Precision@K、MRR 或 nDCG。先看正确片段是否进入候选，再看重排后位置，最后才评估生成答案。若检索阶段没召回，继续调 Prompt 没有意义。

权限过滤必须在检索链路中执行，不能先把全部候选交给模型再要求它别泄露。多租户数据应在索引、过滤和缓存各层保持隔离。`,
            `Embedding 把内容编码到向量空间，通过相似度召回语义相关候选。向量检索不擅长所有精确匹配，通常要结合 BM25、元数据过滤和重排。索引必须管理文档版本、模型版本和权限。评估要把检索与生成拆开，先用 Recall@K 等指标确认正确片段能否被召回；权限过滤必须发生在模型看到内容之前。`,
            [
                {
                    question: '为什么向量检索还需要关键词检索？',
                    direction: '专有名词、编号和精确短语更适合词法匹配，两种召回信号互补。',
                },
                {
                    question: '更换 embedding 模型要注意什么？',
                    direction: '向量空间和维度可能改变，需要版本化并重建索引，不能混用新旧向量。',
                },
                {
                    question: '向量分数可以直接当置信度吗？',
                    direction: '不能直接等同，不同查询和模型的分数分布不同，应通过标注集校准阈值。',
                },
                {
                    question: '权限过滤为什么不能放到生成后？',
                    direction: '模型一旦看到越权内容就已经发生数据暴露，输出过滤无法撤回。',
                },
            ],
        ),
        pitfalls: [
            '只做向量召回，订单号、错误码等精确查询反而搜不到。',
            '文档更新后不清理旧向量，互相冲突的版本同时出现。',
            '先检索全量私有数据，再让模型自行判断哪些可以回答。',
        ],
    },
    {
        slug: 'model-selection-routing-cost',
        module: 'ai',
        title: '模型选型、路由与成本控制',
        summary: '按任务质量、延迟、成本和风险选模型，用路由与预算而不是单一大模型解决全部问题。',
        depth: 'deep',
        heat: 2,
        year: 2026,
        tags: ['模型选型', '路由', '成本'],
        overview:
            '模型越大通常越贵、越慢，但并非每个任务都更好。生产系统应基于评估数据分层路由，并对 token、重试、工具和缓存建立预算。',
        body: createReviewBody(
            `## 先定义任务，再比较模型

把业务拆成分类、抽取、改写、问答、代码、视觉理解、工具调用等任务，不同任务分别建立评估集。比较正确率、稳定性、格式通过率、语言表现、上下文容量、首 token 延迟、吞吐、价格、可用区和数据政策。平均分会掩盖关键失败，应给安全、权限和核心业务样例更高权重。

## 分层路由

简单分类和格式转换可以走小模型；复杂推理、跨文件修改或高难度代码再升级到强模型。路由依据可以是任务类型、输入长度、风险等级、用户套餐和小模型置信度。升级策略必须可观测，避免错误任务被静默分到能力不足的模型。

路由本身也可能失败。规则路由更可控，模型路由更灵活但需要评估。高风险任务不能仅凭模型自报“有信心”决定降级；应结合结构校验、检索覆盖、外部验证和明确风险分类。

## 成本由整条链路决定

成本不只有一次模型调用：输入和输出 token、RAG 重排、embedding、工具调用、重试、评估和日志存储都会计费或消耗资源。长历史要摘要，静态前缀可利用缓存，检索只带必要片段，输出设置合理上限。缓存键必须包含模型、Prompt 版本、权限和相关数据版本，不能让不同用户共享私有答案。

用单请求预算、用户配额和全局熔断限制异常消耗。记录每类任务的成功成本，而不只是每百万 token 单价：便宜模型如果错误率高、重试多，最终可能更贵。

## 降级策略

超时或限流时，可以切备用模型、返回检索结果、缩短非关键输出或进入异步队列。降级不能偷偷降低安全策略，也不能把结构不兼容的模型直接替换进工具链。`,
            `模型选型应按具体任务比较质量、格式、延迟、成本、合规和稳定性，而不是迷信参数量或榜单。生产系统可让小模型处理简单任务，复杂或高风险任务升级，但路由必须经过评估并可观测。成本要算完整链路和成功率，缓存必须包含权限与版本。限流、预算和降级策略要提前设计，降级不能绕过安全边界。`,
            [
                {
                    question: '为什么最便宜的模型不一定成本最低？',
                    direction: '错误、重试和人工修正会提高单次成功任务的总成本。',
                },
                {
                    question: '怎样设计模型升级条件？',
                    direction:
                        '结合任务类型、风险、校验失败、检索覆盖和输入复杂度，不只看模型自报置信度。',
                },
                {
                    question: '答案缓存最容易出现什么安全问题？',
                    direction: '缓存键遗漏用户权限或数据版本，导致跨用户泄露或返回过期内容。',
                },
                {
                    question: '首 token 延迟和总延迟有何区别？',
                    direction:
                        '前者决定用户多久看到响应开始，后者决定完整任务多久结束，两者都要监控。',
                },
            ],
        ),
        pitfalls: [
            '只看通用榜单或 token 单价，没有使用业务评估集。',
            '缓存答案时忽略用户权限、Prompt 和知识版本。',
            '发生限流就切换备用模型，却没验证格式和工具调用兼容性。',
        ],
    },
    {
        slug: 'llm-evaluation-observability',
        module: 'ai',
        title: 'LLM 评估与可观测性',
        summary: '用离线评估守住回归，用线上指标发现真实失败，并保存可复现的链路版本。',
        depth: 'deep',
        heat: 3,
        year: 2026,
        tags: ['Evals', '监控', '可观测性'],
        overview:
            '生成式结果不能只靠单元测试。可靠评估需要代表性样例、任务指标、人工标准、线上反馈，以及模型与上下文链路的版本记录。',
        body: createReviewBody(
            `## 离线评估集怎样建立

从真实任务抽样，覆盖常见请求、长尾、边界、对抗输入、拒答、安全和历史故障。每条样例保存输入、必要上下文、期望行为和评分规则。数据要按场景分层，避免总体平均分掩盖某个重要人群或语言的退化。

确定性任务使用精确匹配、schema 通过率、单元测试或工具执行结果。开放回答可用规则、语义指标、LLM-as-judge 和人工评审组合。模型裁判要有明确 rubric、位置随机化和人工校准；它会偏爱更长、更像自身风格的答案，不能当绝对真值。

## 评估整条系统而不只是模型

RAG 分开测召回、重排、引用和答案忠实度；Agent 测工具选择、参数正确率、任务完成率、步数和副作用；代码生成测编译、测试、行为和安全。每次变更记录模型、参数、Prompt、检索索引、工具 schema 和代码版本，才能复现实验。

发布前与基线做成对比较，并为核心指标设置门槛。随机生成存在波动，重要样例可重复运行，报告均值和失败分布。不要用测试集反复调 Prompt 到过拟合，应保留独立验证集。

## 线上监控

关注任务成功率、拒答率、格式失败率、工具错误率、无结果检索率、首 token 与总延迟、token 和单次成功成本。用户点赞只能作为弱信号，还要结合重试、改写、放弃、人工接管和投诉。

日志默认脱敏并设置保留期限。为一次请求生成 trace id，串联模型调用、检索、工具、重试和最终输出；高风险数据不能为了“方便调试”无限保留。

## 从事故回到评估集

线上失败经过脱敏后，应沉淀成回归样例，并标注故障发生在检索、Prompt、模型、工具还是业务系统。没有这一步，团队只会反复修同类问题。`,
            `LLM 系统要同时做离线评估和线上监控。评估集来自真实任务与历史故障，按场景分层，并根据任务使用结构校验、执行结果、人工标准和经过校准的模型裁判。RAG、Agent 和代码生成要分别评估各环节。线上通过 trace 串起模型、检索、工具和重试，监控成功率、延迟、错误和单次成功成本；事故要回流为回归样例。`,
            [
                {
                    question: 'LLM-as-judge 可以完全替代人工吗？',
                    direction: '不能，需要明确评分标准、人工校准，并持续检查偏差和与人工的一致性。',
                },
                {
                    question: '为什么要把 RAG 拆成多段评估？',
                    direction: '答案错误可能来自没召回、排序错误或生成不忠实，拆开才能定位根因。',
                },
                {
                    question: '用户点赞率为什么不够？',
                    direction: '反馈有选择偏差且含义模糊，需要结合重试、放弃和任务完成等行为。',
                },
                {
                    question: '如何避免评估集过拟合？',
                    direction:
                        '保留独立验证集，持续引入新真实样例，并避免围绕固定答案机械调 Prompt。',
                },
            ],
        ),
        pitfalls: [
            '只评估模型最终答案，不知道问题出在检索、工具还是生成。',
            '把模型裁判分数当绝对真值，没有人工校准。',
            '线上保存完整提示和用户数据，却没有脱敏与保留期限。',
        ],
    },
    {
        slug: 'rag-vs-fine-tuning',
        module: 'ai',
        title: 'RAG、微调与 Prompt 如何选择',
        summary: '知识更新用 RAG，行为与风格稳定可考虑微调，任务定义先从 Prompt 和评估开始。',
        depth: 'deep',
        heat: 2,
        year: 2026,
        tags: ['Fine-tuning', 'RAG', 'Prompt'],
        overview:
            'Prompt、RAG 和微调解决的问题不同。先明确缺的是任务说明、外部知识还是稳定行为，再选择成本最低且可验证的方案。',
        body: createReviewBody(
            `## 三种手段分别解决什么

Prompt 负责告诉模型这次做什么，适合快速定义任务、格式和约束。RAG 在推理时提供外部知识，适合频繁更新、需要引用、按用户权限变化的数据。微调改变模型的行为分布，适合大量稳定样例能够示范的格式、语气、分类边界或工具选择习惯。

不要用微调“灌入”经常变化的产品资料：知识会过期，来源难追踪，更新还要重新训练。也不要期待 RAG 自动改变模型表达风格或复杂决策习惯，它主要解决上下文中有没有相关证据。

## 决策顺序

先建立评估集，用清晰 Prompt 和结构化输出得到基线。如果失败源于缺少私有或最新事实，引入 RAG；如果上下文里已有正确证据但模型长期不按目标行为输出，并且有足够高质量样例，再评估微调。很多系统最终会组合使用：微调负责行为，RAG 负责知识，Prompt 负责当前任务。

## 微调数据与风险

数据质量比数量更重要。样例要代表线上分布，输入输出格式一致，去重、脱敏并划分训练与验证集。错误、冲突和单一风格偏差会被模型学习。训练后不仅测目标能力，还要测通用能力、安全拒答和旧场景是否回退。

微调需要版本管理、训练成本、部署能力和回滚方案。敏感数据是否允许进入训练、供应商是否保留数据、权重归谁管理，都属于上线前的合规决策。

## 何时什么都不做

如果任务可以由确定性代码、搜索、规则引擎或数据库查询可靠完成，就不应为了“AI 化”强行加入模型。模型适合处理模糊语言和开放模式，不适合替代已有的确定性约束。`,
            `Prompt、RAG 和微调解决不同问题：Prompt 定义当前任务，RAG 提供可更新且可引用的外部知识，微调让稳定行为和风格更一致。实践中先建评估集和 Prompt 基线，缺知识再上 RAG，有大量高质量样例且行为仍不稳定才考虑微调。微调数据必须脱敏、去重并测试回退；能用确定性代码可靠解决的问题不必强行使用模型。`,
            [
                {
                    question: '公司文档应该通过微调放进模型吗？',
                    direction: '通常优先 RAG，因为文档会更新、需要权限和引用；微调不适合当知识库。',
                },
                {
                    question: '什么信号说明可以考虑微调？',
                    direction:
                        '上下文已包含正确信息，但稳定行为仍达不到要求，并且拥有足够一致的高质量样例。',
                },
                {
                    question: '微调后只测目标任务够吗？',
                    direction: '不够，还要检查安全能力、通用能力和既有场景是否退化。',
                },
                {
                    question: 'RAG 与微调能一起用吗？',
                    direction: '可以，前者提供知识，后者塑造行为，Prompt 再描述当前任务。',
                },
            ],
        ),
        pitfalls: [
            '用微调记忆频繁变化的业务文档，更新困难且无法可靠引用。',
            '没有基线和评估集就开始微调，训练后无法判断是否改善。',
            '把本可由确定性代码完成的规则判断交给生成模型。',
        ],
    },
    {
        slug: 'ai-frontend-architecture',
        module: 'ai',
        title: 'AI 应用的前端架构与 BFF 边界',
        summary: '浏览器负责交互，BFF 负责密钥、鉴权、模型适配与流式转发，边界不能倒置。',
        depth: 'core',
        heat: 3,
        year: 2026,
        tags: ['BFF', 'AI Gateway', '前端架构'],
        overview:
            '典型链路是浏览器 → BFF/AI Gateway → 模型、检索与工具。前端不能持有厂商密钥，也不应直接承担租户权限、模型路由和敏感日志处理。',
        body: createReviewBody(
            `## 为什么需要 BFF

浏览器直连模型接口看似简单，却会暴露服务密钥，也难以统一处理用户鉴权、额度、审计、模型切换和内容过滤。更合理的结构是：浏览器只携带自己的登录态，请求业务 BFF；BFF 校验当前用户和资源权限，再调用模型、检索服务或工具。

BFF 不是简单代理。它负责把前端稳定的业务协议转换成不同厂商的模型协议，隐藏模型名称、密钥和供应商差异。前端依赖 message、delta、citation、tool_call、usage、error 等业务事件，不直接绑定某家模型的原始响应格式。

## 请求链路怎样设计

开始请求时生成 request id 或 trace id。BFF 完成身份校验、配额检查、Prompt 组装和模型路由，然后以流式事件返回。前端按事件类型更新状态，并允许 AbortController 取消。取消不仅要停止浏览器读取，还要由服务端终止下游生成或标记任务取消。

短问答适合同步流式连接；耗时 Agent、批量分析和文件处理适合异步任务：创建任务后返回 job id，前端订阅进度或轮询状态。页面刷新后可凭 job id 恢复，而不是依赖一条永远不关闭的 HTTP 连接。

## 稳定的前后端合同

事件需要版本、事件 id、类型、时间和 payload。客户端要容忍未知事件，防止服务端新增类型后旧前端直接崩溃。错误也应结构化：区分未登录、额度不足、输入非法、模型限流、工具失败和可重试的暂时错误，不要只返回一段字符串。

流被中断时，前端要知道这条消息是 generating、completed、stopped 还是 failed。若支持断线续传，服务端需保存事件序号，客户端从 last event id 之后恢复，并对重复事件去重。

## 缓存、限流与降级

静态模型列表和公共配置可以缓存，私有回答不能只按 Prompt 文本缓存，缓存键还要包含用户权限、知识版本、模型和 Prompt 版本。限流信息应转成可理解的等待或降级状态。备用模型的事件格式和工具能力必须兼容，不能在前端无感切换后得到完全不同的数据结构。

## 前端应该知道多少内部细节

前端需要展示用户能理解的状态、来源和操作步骤，但不应该拿到系统 Prompt、服务密钥、内部堆栈或完整工具凭证。调试信息进入受权限保护的观测系统，面向用户只返回安全的错误码、说明和 trace id。`,
            `AI 前端通常通过 BFF 或 AI Gateway 调用模型。浏览器管理交互、流式渲染和取消；BFF 管理密钥、鉴权、Prompt、路由、配额和审计。前后端用稳定的业务事件协议解耦模型厂商。短任务走同步流，长任务走可恢复的异步 job。缓存和断线续传必须考虑权限、版本、事件去重与明确状态。`,
            [
                {
                    question: '为什么不能让浏览器直接调用模型？',
                    direction: '会暴露服务密钥，并让鉴权、额度、审计和供应商适配散落在客户端。',
                },
                {
                    question: '什么时候使用异步 job？',
                    direction: '任务可能超过网关连接时间、需要后台继续运行或页面刷新后恢复时。',
                },
                {
                    question: '为什么前端不应依赖厂商原始事件？',
                    direction:
                        '供应商格式变化或切换模型会影响所有页面，应由 BFF 转成稳定业务协议。',
                },
                {
                    question: '断线续传需要哪些条件？',
                    direction: '服务端保存可重放事件和序号，客户端携带最后事件 id 并处理重复事件。',
                },
            ],
        ),
        pitfalls: [
            '浏览器直接携带模型厂商密钥发请求。',
            '前端绑定某家模型的原始流格式，切换供应商时全站修改。',
            '长任务依赖单条 HTTP 连接，页面刷新后状态和结果全部丢失。',
        ],
    },
    {
        slug: 'ai-product-interaction',
        module: 'ai',
        title: 'AI 产品交互设计',
        summary: '让生成过程可见、可控、可修正，并清楚表达来源、不确定性和失败状态。',
        depth: 'core',
        heat: 3,
        year: 2026,
        tags: ['AI UX', '交互设计', '人机协作'],
        overview:
            'AI 交互不是“输入框加聊天气泡”。非确定输出、长延迟、工具副作用和可能失败，要求界面提供状态、控制、引用、编辑与恢复能力。',
        body: createReviewBody(
            `## AI 交互与普通表单的差别

普通表单通常是确定输入对应确定结果；AI 输出可能变化、耗时更长，也可能只完成一部分。界面必须让用户知道系统正在理解、检索、生成、调用工具还是等待确认，不能只放一个无限旋转的 loading。

生成前帮助用户明确输入：示例、模板、附件说明和能力边界比空白输入框更有效。生成中支持停止；生成后支持复制、编辑、重新生成、基于当前结果继续，以及明确反馈哪里不对。

## 状态必须准确

一条回答至少区分 idle、submitting、streaming、waiting_for_approval、completed、stopped 和 failed。停止的半成品不能显示成成功；网络断开也不能自动等同于模型失败。若可以恢复，显示“继续接收”；不能恢复则保留已生成内容并提供重试。

重新生成会产生新版本，不应静默覆盖用户已经引用或编辑的旧版本。复杂产品可以保留分支关系，让用户知道后续对话基于哪个版本。

## 引用与可信度

涉及事实或企业知识时，把引用放到具体段落或结论附近，显示标题、来源位置、更新时间和访问状态。引用存在不等于支持结论，前端应允许展开原文核对。来源失效或用户无权访问时，要明确说明，而不是留下不可点击的编号。

不要用一个看似精确的“AI 置信度 93%”制造确定感，除非这个分数经过任务级校准。更实用的表达是：是否找到来源、是否通过校验、哪些部分是推断、下一步如何核实。

## 工具调用和副作用

只读工具可以展示步骤摘要；发消息、删除、付款、发布、改权限等副作用需要确认页，列出对象、参数、影响和是否可撤销。用户确认的是最终操作，不是模型的自然语言承诺。执行后返回真实系统结果，并允许查看失败原因或补救动作。

## 空态、拒答和失败

没有检索结果时提供修改问题、调整范围或上传资料的入口。模型拒答要说明能力或政策边界，并给安全替代方案。错误文案区分用户可修复问题与系统故障；前者指出具体字段，后者提供重试、稍后处理和 trace id。

## 无障碍与性能

流式更新不要让屏幕阅读器逐 token 重复播报，应按语义段落合并通知。自动滚动仅在用户位于底部时继续，用户向上阅读后不要抢夺位置。长回答需要虚拟化、折叠步骤和渐进渲染，同时保持停止按钮随时可达。`,
            `AI 交互需要把非确定、慢和可能失败的生成过程变得可见、可控、可恢复。前端应准确区分提交、流式、等待确认、完成、停止和失败，支持停止、编辑、重试和版本保留。事实回答展示可核对引用，不伪造置信感；副作用操作在执行前展示最终参数并确认。还要处理空态、拒答、无障碍和长内容性能。`,
            [
                {
                    question: '为什么重新生成不应直接覆盖旧答案？',
                    direction: '旧答案可能已被编辑或用于后续讨论，保留版本才能追踪上下文和恢复。',
                },
                {
                    question: '怎样展示 AI 的不确定性？',
                    direction: '展示证据覆盖、校验结果和推断范围，比未经校准的百分比更可信。',
                },
                {
                    question: '自动滚动什么时候应该停止？',
                    direction: '用户主动向上阅读后停止，只有用户回到底部或点击跟随时恢复。',
                },
                {
                    question: '工具确认页应展示什么？',
                    direction: '真实执行对象、关键参数、影响范围、费用或风险，以及能否撤销。',
                },
            ],
        ),
        pitfalls: [
            '所有阶段只显示同一个 loading，用户不知道是在生成还是卡住。',
            '重新生成直接覆盖旧版本，后续上下文和用户修改无法追踪。',
            '用未经校准的百分比装饰答案，让用户误以为结论经过验证。',
        ],
    },
    {
        slug: 'agent-ui-client-state',
        module: 'ai',
        title: 'Agent UI、工具调用与客户端状态机',
        summary: '把消息、运行、步骤和工具审批拆开建模，正确处理并发、重放与恢复。',
        depth: 'deep',
        heat: 3,
        year: 2026,
        tags: ['Agent UI', '状态机', 'Tool Call'],
        overview:
            'Agent 不是一段不断增长的文本。前端要同时表达一次运行中的多个步骤、工具调用、审批、结果和最终回答，并保证事件可重放。',
        body: createReviewBody(
            `## 先区分四种对象

Conversation 表示会话容器；Message 表示用户或助手内容；Run 表示一次从输入到完成的执行；Step 表示 run 内的模型、检索或工具步骤。工具调用还应有独立 id、参数、审批状态和执行结果。把所有内容塞进 message.content，会让恢复、重试和审计变得困难。

一条用户消息可以触发多个 run，例如重新生成；一个 run 可以包含多个并行工具。最终回答属于某个 run，而不是简单追加到最后一条消息。客户端状态应规范化存储，通过 id 建立关系，避免深层数组在流式更新时频繁复制和错位。

## 用状态机约束非法状态

Run 可以从 queued 到 running，再进入 waiting_for_approval、completed、failed、cancelled。Tool call 可以是 proposed、approved、rejected、executing、succeeded、failed。只有 proposed 能批准，只有运行中的任务能取消。状态转移由事件驱动，不要散落成多个互相冲突的布尔值。

按钮是否可用由状态推导：等待审批时显示批准与拒绝；执行中禁止重复批准；已取消 run 的晚到 delta 应忽略或归档，不能继续写进当前气泡。

## 事件顺序与幂等

每个事件包含 run id、step id、event id 和递增序号。重连后服务端可能重放事件，reducer 必须按 event id 去重。并行工具的结果可能乱序返回，不能假设到达顺序就是步骤顺序。结束事件到达后若还有晚到片段，应按协议决定丢弃或记录异常。

用户双击确认、网络自动重试或多个标签页操作时，审批请求要携带幂等键。服务端才是最终状态来源；前端乐观更新后也必须接受服务端纠正。

## 历史与记忆的前端边界

聊天历史是用户可见记录，任务状态是本次执行数据，长期记忆是系统跨会话保存的信息，三者不能混为一谈。前端应允许用户查看、修改或删除长期记忆，并明确哪些信息会被下次会话使用。

多 Agent 的内部讨论通常不需要逐字展示。界面应展示对用户有意义的计划、当前负责人、工具动作和结果摘要，避免用大量“思考过程”制造噪声或泄露内部信息。

## 恢复与多端一致性

刷新页面后先加载服务端快照，再从最后序号订阅增量事件。多个标签页可通过服务端版本号或本地广播协调，但不能让本地缓存覆盖更新的服务端状态。失败恢复需要区分重试整个 run、重试单个安全步骤和从检查点继续。`,
            `Agent 前端应把会话、消息、运行和步骤分开建模，并为工具调用建立独立审批与执行状态。使用明确状态机限制非法操作，通过 run id、event id 和序号处理并发、乱序、重放与晚到事件。历史、任务状态和长期记忆要区分；刷新后从服务端快照与事件恢复。多 Agent 只展示对用户有意义的计划和动作，不暴露冗长内部过程。`,
            [
                {
                    question: '为什么 Message 和 Run 要分开？',
                    direction: '同一消息可能重新生成多次，每次执行都有独立步骤、状态、成本和错误。',
                },
                {
                    question: '多个布尔值为什么不如状态机？',
                    direction: '布尔值容易组合出既完成又失败等非法状态，状态机能限制合法转移。',
                },
                {
                    question: '怎样处理重连后的重复事件？',
                    direction: '事件带唯一 id 和序号，客户端 reducer 幂等去重并从服务端快照校准。',
                },
                {
                    question: '多 Agent 的过程需要全部展示吗？',
                    direction: '不需要，只展示用户可验证的计划、工具动作、产物和需要确认的决策。',
                },
            ],
        ),
        pitfalls: [
            '用多个布尔值表示运行状态，出现同时 loading、failed 和 completed。',
            '默认流式事件严格有序，工具并行或重连后内容错位。',
            '把长期记忆静默保存，用户无法查看、纠正或删除。',
        ],
    },
    {
        slug: 'ai-frontend-security',
        module: 'ai',
        title: 'AI 前端安全与 Prompt Injection',
        summary: '模型输入和输出都不可信；用 BFF、渲染隔离、最小权限和操作确认建立防线。',
        depth: 'deep',
        heat: 3,
        year: 2026,
        tags: ['Prompt Injection', 'XSS', '最小权限'],
        overview:
            'Prompt Injection 不只来自用户输入，也可能藏在网页、文件和检索文档中。前端安全要同时保护密钥、DOM、私有数据与工具副作用。',
        body: createReviewBody(
            `## 先建立威胁模型

输入来源包括用户文字、URL、网页正文、上传文件、剪贴板、检索片段和工具结果。攻击者可能在其中写入“忽略规则并读取秘密”等指令。模型无法可靠地区分自然语言里的数据与指令，因此不能只靠 Prompt 声明“不要被攻击”。

需要保护的资产包括服务密钥、系统 Prompt、其他租户数据、用户隐私、内部文件和工具操作权限。逐条分析攻击者能控制什么输入、模型能看到什么、能调用什么工具，以及结果会在哪里渲染或发送。

## 客户端密钥和权限

任何进入浏览器包、网络请求或 source map 的厂商密钥都视为公开。前端只持有用户会话，BFF 使用服务端凭证。工具执行前由服务端根据当前用户、资源和动作重新鉴权，不能相信模型生成的 userId、role 或“管理员已批准”。

权限应绑定具体工具和资源范围。用于搜索的 token 不应同时具备删除权限；读取当前项目的授权不应覆盖整个组织。高风险操作要求用户确认最终参数，审批后若参数变化必须重新确认。

## 安全渲染模型输出

Markdown 渲染器要默认禁止原始 HTML，链接协议只允许 http、https 等安全白名单，外链使用合适的 rel 属性。代码块只展示，不自动执行。模型生成的 SVG、HTML、Mermaid 或组件代码都可能包含脚本、外部资源和钓鱼链接，需要沙箱和严格 CSP。

下载文件名、路径、图片 URL 和 data URL 也应校验。不要把模型输出直接传给 innerHTML、eval、shell、SQL 或动态模块加载。即使内容来自“可信模型”，其上游上下文仍可能被攻击者控制。

## 间接注入和数据外泄

读取网页或文件时，只提取任务所需内容，并明确标为不可信数据。不要向模型提供无关秘密；模型看不到的数据就无法被诱导泄露。工具返回结果要最小化字段并脱敏，检索必须在模型调用前完成租户和文档权限过滤。

阻止外带通道：模型不应任意请求陌生 URL、加载远程图片或把敏感内容拼进链接。网络工具使用域名白名单、请求方法限制和响应大小限制。

## 审批、审计与失败方式

读操作与写操作分开。发送、删除、发布、购买和改权限需要预览、确认、幂等键和审计记录。遇到无法判断的权限或注入风险时应失败关闭，不以“尽量完成任务”为理由绕过限制。安全日志记录动作和资源 id，但不复制完整敏感内容。`,
            `AI 前端要把用户输入、网页、文件、检索内容和模型输出全部当作不可信数据。密钥留在 BFF，工具在服务端按当前用户重新鉴权并坚持最小权限。Markdown、链接、HTML 和生成文件要安全渲染，高风险副作用展示最终参数并确认。防 Prompt Injection 的核心是减少模型可见秘密、隔离指令与数据、限制工具和外带通道，而不是多写一句系统提示。`,
            [
                {
                    question: 'Prompt Injection 为什么无法只靠 Prompt 解决？',
                    direction:
                        '攻击与正常内容都使用自然语言，模型不能成为权限边界，必须依赖系统隔离和服务端控制。',
                },
                {
                    question: 'Markdown 渲染的主要风险是什么？',
                    direction:
                        '原始 HTML、危险链接协议、外部资源和可执行图表可能造成 XSS 或数据外带。',
                },
                {
                    question: '为什么工具执行时还要重新鉴权？',
                    direction: '模型参数可能被伪造或注入，只有服务端掌握可信用户身份和资源权限。',
                },
                {
                    question: '最有效的数据防泄露原则是什么？',
                    direction: '不要把当前任务不需要的秘密、字段和文档提供给模型或工具。',
                },
            ],
        ),
        pitfalls: [
            '认为 system prompt 优先级高，所以足以阻止所有提示注入。',
            'Markdown 禁止 script 后就直接允许任意 HTML、链接和远程图片。',
            '工具相信模型传入的用户身份和审批状态，没有服务端重新鉴权。',
        ],
    },
    {
        slug: 'ai-frontend-testing',
        module: 'ai',
        title: 'AI 前端测试策略',
        summary: '把流式协议和状态机测确定，把模型输出按性质选择结构校验、行为评估或人工验收。',
        depth: 'core',
        heat: 3,
        year: 2026,
        tags: ['前端测试', 'Mock', '流式测试'],
        overview:
            'AI 输出具有概率性，但前端协议、状态转移、权限确认和渲染行为仍然可以确定测试。关键是不要把所有测试都写成完整文本匹配。',
        body: createReviewBody(
            `## 先把系统拆成可测层

第一层是纯 UI：消息、引用、工具卡片和错误组件。第二层是状态 reducer：事件怎样把 run 从 queued 推到 completed。第三层是协议适配：字节流怎样解析成业务事件。第四层是页面集成：提交、停止、重试和审批。第五层才是连接真实模型的端到端与质量评估。

越靠前的测试越快、越确定；越靠后的测试越接近真实系统但成本更高、波动更大。不要用真实模型承担本该由组件测试覆盖的 loading、按钮禁用和错误文案。

## 如何 Mock 流式响应

测试服务器按可控顺序发送 metadata、delta、citation、tool_call 和 done。除了正常路径，还要覆盖一个 JSON 事件被拆成多个网络 chunk、多个事件合在一个 chunk、UTF-8 多字节字符被截断、心跳、未知事件和半途断线。网络 chunk 边界不等于消息边界，这是流式解析最常见的错误来源。

使用假时钟测试超时、退避和防抖，使用 AbortSignal 验证停止后不再更新 UI。模拟重连重放同一 event id，确认 reducer 不会重复追加；模拟两个工具乱序完成，确认结果仍归属正确 step。

## 组件与交互测试

测试可观察行为：用户点击停止后状态变为 stopped、文本冻结、按钮变化，并发请求不会串消息。工具确认要验证最终参数可见、拒绝不执行、双击不会重复提交。引用应能定位来源，失效来源有明确状态。

Markdown 使用专门的恶意样例测试 raw HTML、javascript 链接、图片外带和超长代码块。无障碍测试检查焦点、键盘操作、aria-live 和流式更新不会反复朗读整段内容。

## 怎样测试概率性输出

机器要消费的字段用 schema 和业务不变量测试，不比较属性顺序或整段文本。代码生成可以执行类型检查和测试；事实问答检查引用覆盖与忠实度；开放文案使用评分规则和抽样人工评审。真实模型测试设置独立额度并记录模型版本，避免每次前端提交都跑昂贵且不稳定的全量评估。

## 视觉回归与端到端

固定事件夹具可以稳定截图 streaming、waiting、failed 和超长内容状态。端到端测试覆盖从提交到完成、取消、刷新恢复、审批和权限拒绝。生产前再用少量真实模型做冒烟测试，确认供应商协议和 BFF 适配没有漂移。`,
            `AI 前端应把确定性的 UI、状态机和流协议，与概率性的模型质量分开测试。使用可编排的流式 Mock 覆盖拆包、粘包、UTF-8 截断、断线、重放和乱序；组件测试关注停止、重试、审批、引用与安全渲染。结构化结果验证 schema 和业务不变量，真实模型只用于质量评估和少量集成冒烟，并记录模型版本与成本。`,
            [
                {
                    question: '为什么不能把每次 delta 当成完整 JSON？',
                    direction:
                        '网络分块边界是任意的，一个事件可能被拆开，多个事件也可能合并到同一块。',
                },
                {
                    question: '怎样避免 AI 测试频繁波动？',
                    direction:
                        '前端测试使用固定事件夹具，真实模型质量测试单独运行并采用容忍概率性的评分方法。',
                },
                {
                    question: '停止功能需要验证哪些行为？',
                    direction:
                        'AbortSignal 被触发、服务端收到取消、UI 冻结且晚到事件不再污染当前结果。',
                },
                {
                    question: '为什么需要安全渲染测试？',
                    direction: '模型输出和引用来源都可能包含恶意 HTML、链接或远程资源。',
                },
            ],
        ),
        pitfalls: [
            '所有模型回答都用完整字符串快照，轻微措辞变化就导致测试失败。',
            'Mock 每次只返回完整事件，没有覆盖真实网络拆包与断线。',
            '只测成功回答，不测停止、重放、乱序、审批拒绝和危险 Markdown。',
        ],
    },
    {
        slug: 'ai-production-troubleshooting',
        module: 'ai',
        title: 'AI 生产故障分层排查',
        summary: '从浏览器到 BFF、模型、检索和工具逐层确认，先定位故障域，再调整 Prompt。',
        depth: 'deep',
        heat: 3,
        year: 2026,
        tags: ['故障排查', 'Trace', '可观测性'],
        overview:
            'AI 应用链路长，表现为“回答不对”的问题可能来自前端丢事件、Prompt 版本、检索权限或工具失败。分层证据比反复重试更重要。',
        body: createReviewBody(
            `## 第一步：固定一次失败请求

先拿到时间、用户可公开描述、conversation id、run id、trace id、前端版本和复现步骤。保存脱敏后的事件时间线，不要仅凭截图猜测。确认是所有用户、特定租户、特定浏览器、特定输入还是偶发问题，再判断影响范围和是否需要降级或回滚。

同一个问题重复发送可能得到不同答案，所以复现时要固定模型、参数、Prompt 版本、知识索引版本和工具 schema。没有版本信息就无法区分随机波动与系统变更。

## 前端层

检查请求是否真的发出，身份和参数是否正确；响应状态、content-type 和代理是否支持流。查看字节是否持续到达、解析器是否丢失拆分事件、event id 是否重复、状态机是否忽略晚到事件。页面卡住可能只是主线程被 Markdown 重解析阻塞，不一定是模型慢。

若刷新后内容丢失，检查 run 是否落到服务端、客户端是否使用正确 id 恢复。若回答串到别的会话，重点检查并发请求的闭包、AbortController 复用、列表 key 和状态归属。

## BFF 与网关层

通过 trace id 查看鉴权、配额、模型路由、超时、重试和取消传播。确认中间代理是否缓冲 SSE、修改响应头或提前关闭连接。关注首字节时间、首 token 时间和完整耗时：前两者异常通常指向排队、网络或上游；只有渲染慢则回到前端性能。

重试可能把一次写操作执行多次，也可能让成本暴涨。检查幂等键、重试次数、退避和熔断。错误映射不能把供应商限流都伪装成“生成失败”。

## 模型与 Prompt 层

确认实际使用的模型、参数、系统规则和模板版本。查看上下文是否超长被截断，关键规则是否进入窗口，结构化输出 schema 是否与客户端一致。用同一上下文在隔离环境重放；如果稳定失败再调整 Prompt，不能一看到坏答案就先加一句规则。

模型层常见信号包括格式校验失败、拒答异常、输出提前停止、延迟或错误率在模型版本切换后突变。比较基线评估和最近变更，必要时回滚模型或 Prompt。

## 检索层

先看正确文档是否有权限且已进入索引，再看查询改写、过滤条件、召回结果、分数、重排和最终送进模型的片段。正确片段没召回是 recall 问题；召回却排在窗口外是排序或预算问题；片段在上下文中但回答歪曲，才是生成忠实度问题。

文档更新未生效时检查采集任务、切片版本、embedding 模型和旧向量清理。跨租户结果属于安全事故，应立即限制访问并检查索引、过滤和缓存，而不是仅过滤前端展示。

## 工具层

核对模型选择的工具、参数校验、服务端鉴权、真实请求和返回值。区分“模型没调用工具”“参数生成错误”“工具自身失败”“结果没有回传模型”和“前端没有展示”。对副作用工具检查幂等记录，防止排障重放再次执行。

## 形成结论

结论应包括根因层级、触发条件、用户影响、临时缓解、永久修复和防复发措施。把脱敏故障样例加入测试或评估集，并增加能直接捕捉该故障的指标。只说“模型偶发不稳定”通常意味着证据还不充分。`,
            `AI 生产故障要先固定一次失败请求和完整版本信息，再沿前端、BFF、模型与 Prompt、检索、工具分层排查。前端看请求归属、流解析、状态机和渲染；网关看鉴权、缓冲、超时和重试；模型看真实上下文与版本；检索拆分召回、排序和生成忠实度；工具拆分选择、参数、执行和回传。根因确认后将事故沉淀为测试、评估样例和监控指标。`,
            [
                {
                    question: '为什么坏答案不能直接归因于模型？',
                    direction:
                        '正确上下文可能没被检索、被截断，工具也可能失败，前端甚至可能展示了错误 run。',
                },
                {
                    question: '首 token 慢与页面渲染慢怎样区分？',
                    direction:
                        '比较服务端事件时间和浏览器收到、绘制时间，判断延迟发生在上游还是主线程。',
                },
                {
                    question: 'RAG 错误怎样定位到具体阶段？',
                    direction: '依次检查索引、权限、召回、重排、上下文拼装和答案对证据的忠实度。',
                },
                {
                    question: '排障重放工具调用有什么风险？',
                    direction: '副作用可能被再次执行，必须使用隔离环境、只读模式或相同幂等键。',
                },
            ],
        ),
        pitfalls: [
            '看到答案错误就继续堆 Prompt，没有检查真实上下文和检索结果。',
            '没有 trace id 和版本信息，只靠用户截图反复猜测。',
            '在生产环境重放有副作用的工具调用，造成二次影响。',
        ],
    },
    {
        slug: 'advanced-ai-assisted-frontend-coding',
        module: 'ai',
        title: 'AI 辅助前端编码进阶',
        summary: '用需求合同驱动仓库级修改，控制跨文件影响，并安全完成迁移与重构。',
        depth: 'deep',
        heat: 3,
        year: 2026,
        tags: ['需求驱动', '仓库级修改', '迁移重构'],
        overview:
            '仓库级任务的难点不是生成代码，而是理解现有合同、规划改动顺序、持续验证和避免范围失控。AI 需要在证据和护栏内工作。',
        body: createReviewBody(
            `## 从需求合同开始

把需求写成可观察行为：谁在什么状态下执行什么操作，页面应展示什么，失败时如何恢复。列出不变量，例如 URL 兼容、埋点名称、接口字段、键盘操作和权限行为。每条验收标准对应测试或人工检查，不让“页面看起来完成”替代业务验收。

让 AI 先输出对需求的复述、未知点和影响清单，而不是马上写代码。业务含义不明确时由人决定；实现细节如果能从仓库约定推断，可以继续读取证据。

## 建立仓库地图

从路由或组件入口向下追踪状态、请求、类型和样式，再反向搜索组件的所有调用方。识别公共组件、设计系统、数据层、测试工具和生成代码。检查 package、TypeScript、lint、构建配置及仓库规则，确认实际框架版本和可用 API。

仓库很大时只加载当前任务需要的切片：入口、直接依赖、调用方、公共类型和相邻测试。先搜索已有 hook、组件和工具，防止 AI 因为没看到全局实现而重复造轮子。

## 规划跨文件改动

按依赖方向安排步骤：先确定类型和合同，再改底层逻辑，随后改消费者，最后清理旧路径。每一步保持可构建，必要时使用兼容层或 feature flag 让迁移可逐步发布。提前列出预计修改文件和原因，出现计划外文件时重新评估影响。

不要一次让 AI 输出巨型补丁。小批次差异更容易发现错误、运行针对性测试和回退。纯机械迁移可以扩大批次，但要先用少量代表文件验证规则，并检查自动替换无法理解的动态用法。

## 前端重构的特殊风险

组件重构不仅要保持 DOM 外观，还要保持焦点、键盘、aria、事件冒泡、受控状态和表单提交。状态管理迁移要检查订阅粒度、并发更新、持久化和服务端渲染水合。请求层迁移要保持缓存键、取消、重试和错误语义。样式迁移要检查层叠、响应式、主题和视觉回归。

性能优化必须有证据。AI 常会无差别加入 memo、缓存和抽象，反而增加复杂度或陈旧状态。先用 profiler、网络瀑布或 bundle 分析确认瓶颈，再做可测量修改。

## 依赖与框架升级

先读官方迁移说明和仓库当前用法，列出破坏性变化。锁定升级范围，检查 lockfile 差异、浏览器目标、构建插件和类型版本。使用弃用警告和 codemod 做机械部分，但对运行时行为、第三方组件和样式逐项验证。

大升级采用双轨或分阶段策略：先兼容新旧接口，再迁移调用方，最后删除兼容层。每一步有回滚点，不能把升级、业务功能和无关重构混进同一个难以审查的变更。

## 验证与交付

验证顺序是最相关测试、类型、lint、构建、页面交互、视觉与无障碍，最后审查完整差异。记录哪些命令实际运行、哪些场景人工确认、哪些环境无法覆盖。AI 的总结不算证据，终端结果、截图、测试和真实行为才算。

最终交付应说明需求如何对应改动、公共行为是否变化、迁移和回滚方式、剩余风险与监控指标。高影响代码由熟悉该领域的人复核。`,
            `AI 处理前端仓库级任务时，应从可观察需求和不变量出发，先建立入口、依赖、调用方、类型与测试的仓库地图。跨文件修改按合同到消费者的依赖顺序小步推进，并设置变更预算和回滚点。重构要保留无障碍、状态、请求缓存和样式行为；升级要依据官方迁移说明分阶段完成。最终以实际测试、构建、交互和差异为证据，而不是相信模型总结。`,
            [
                {
                    question: '仓库级修改前最先搜索什么？',
                    direction: '入口、调用方、公共类型、已有同类实现、相邻测试和仓库规则。',
                },
                {
                    question: '为什么跨文件修改要保持每一步可构建？',
                    direction: '能快速定位回归、独立审查和回退，避免最后面对大量相互依赖的错误。',
                },
                {
                    question: '组件重构只做截图对比够吗？',
                    direction: '不够，还要验证状态、事件、焦点、键盘、ARIA、表单和响应式行为。',
                },
                {
                    question: '怎样防止 AI 过度优化？',
                    direction: '要求先提供 profiler、网络或包体积证据，并比较优化前后的指标。',
                },
            ],
        ),
        pitfalls: [
            '没有追踪调用方就修改公共组件或类型，造成隐蔽回归。',
            '把框架升级、业务功能和无关重构放进同一个巨大补丁。',
            '只验证页面截图，遗漏焦点、键盘、请求缓存和状态语义变化。',
        ],
    },
    {
        slug: 'ai-assisted-coding',
        module: 'ai',
        title: '使用 AI 写代码：先给上下文，再要实现',
        summary: '把 AI 当不可靠但高效率的协作者：任务拆小、上下文给足、逐步验证。',
        depth: 'intro',
        heat: 3,
        year: 2026,
        tags: ['AI 编程', '上下文', '验证'],
        overview:
            'AI 能加速样板代码、局部重构、测试和解释，但不会天然理解仓库约定。高质量结果依赖明确任务、相关上下文、小步修改和可执行验证。',
        body: createReviewBody(
            `## 适合交给 AI 的工作

优先交付边界清楚、结果可验证的任务：补单元测试、实现纯函数、按现有模式新增组件、解释报错、机械迁移和生成初稿。涉及核心架构、权限、资金、隐私、数据迁移或不可逆操作时，AI 可以辅助分析，但不能代替负责人决策。

## 怎样描述任务

给目标、现状、约束和验收标准，而不是只说“帮我优化”。指出相关文件、可复用实现、不能改变的公开接口、需要覆盖的边界情况，以及应该运行哪些检查。先让 AI 阅读局部上下文，再修改；大任务拆成可以独立验证的小步。

例如：“沿用现有表单校验方式，为注册页增加手机号字段；不改接口协议；覆盖空值、非法格式和服务端报错；通过现有类型检查和测试。”这比“写个手机号注册”更不容易跑偏。

## 协作过程

要求先搜索已有能力，避免重复造工具。限制修改范围，查看每次差异，再继续下一步。对不确定的库版本、API 和业务规则，要求从仓库或官方文档取证，不能凭印象生成。

一个可靠的工作流可以分为六步：先复述需求与假设；再读取仓库规则和相关调用链；给出最小修改方案；逐步实现并查看差异；运行从局部到全局的检查；最后总结改动、验证证据和剩余风险。需求发生变化时重新确认边界，不要让旧计划继续惯性执行。

## 给 AI 的任务模板

可以固定写成：“目标是什么；当前行为是什么；允许修改什么；禁止改变什么；已有实现在哪里；输入输出和异常是什么；完成后运行哪些检查；哪些情况必须先询问”。这不是为了写更长的 Prompt，而是把影响正确性的隐含信息显式化。

## 如何判断是否适合继续自治

可以用三个维度判断：影响范围、可验证性、可回滚性。局部函数、测试充分、能随时回退的任务适合连续执行；跨模块协议、测试缺失但可人工验收的任务应分阶段确认；生产数据、权限、支付、迁移和删除属于高风险任务，需要明确授权、预览和回滚方案。

不要把密钥、生产数据和未经脱敏的日志发给模型。也不要复制一段看起来合理的代码就直接上线；能编译只说明语法和类型大致成立，不说明业务正确。代码所有权仍属于团队：可读性、许可证、依赖来源和后续维护都要由人负责。`,
            `使用 AI 写代码的关键不是一句“写得更好”，而是提供目标、相关上下文、硬约束和验收标准。适合把边界清楚、能自动验证的小任务交给 AI，大任务要拆步。生成前先搜索复用，生成后看差异、跑检查；涉及架构、安全和不可逆操作仍由人决策。密钥和生产数据不能进入提示，模型不确定的 API 必须查证。`,
            [
                {
                    question: '哪些任务最适合第一次交给 AI？',
                    direction:
                        '从纯函数、测试、样板代码和局部重构开始，因为输入输出明确且容易验证。',
                },
                {
                    question: '上下文是不是给得越多越好？',
                    direction:
                        '不是，只给任务相关的入口、类型、调用方、约定和错误信息；无关内容会稀释重点。',
                },
                {
                    question: 'AI 说已经完成，可以直接相信吗？',
                    direction:
                        '不能，以实际差异和检查结果为准；“已完成”只是模型陈述，不是验证证据。',
                },
                {
                    question: '遇到大功能应该一次生成吗？',
                    direction: '先确定方案和边界，再按可独立验收的步骤实现，每一步都保留回退点。',
                },
            ],
        ),
        pitfalls: [
            '只描述想要的页面效果，没有说明现有接口、状态和验收标准。',
            '一次让 AI 跨很多模块重写，最后无法判断是哪一步引入回归。',
            '把密钥、生产数据或未经脱敏的日志直接放进提示。',
        ],
    },
    {
        slug: 'ai-coding-constraints-boundaries',
        module: 'ai',
        title: 'AI 编码约束与边界',
        summary: '把“不能变什么、只能动哪里、怎样算完成”写成可检查的合同。',
        depth: 'core',
        heat: 3,
        year: 2026,
        tags: ['约束', '边界', '验收标准'],
        overview:
            '有效边界不是笼统的“谨慎修改”，而是明确作用域、行为合同、技术规则、风险权限和完成条件，并尽可能由类型、测试和工具自动执行。',
        body: createReviewBody(
            `## 五类边界

第一是作用域：允许修改哪些目录、文件和模块，哪些只读。第二是行为：必须保持哪些 API、数据格式、交互和兼容性。第三是技术：语言版本、依赖策略、架构分层、性能预算和代码规范。第四是权限：是否允许联网、安装依赖、改数据库、发消息、部署或删除资源。第五是完成条件：需要通过哪些测试、构建、安全检查和人工验收。

## 如何确定边界

从真实风险倒推。先找入口、调用方、公共类型、持久化数据和现有测试，再识别改动的影响面。低风险、可回退、自动验证充分的局部修改可以让 AI 自主完成；高影响、难回滚或业务含义不明确的决定必须停下来由人确认。

约束要具体且可判定。“保持代码优雅”无法验收；“不新增依赖、不改变 \`UserResponse\`、只修改 \`src/profile\`、错误码保持兼容、通过现有检查”才可执行。规则有冲突时要定义优先级：安全与数据完整性高于交付速度，仓库规则高于通用偏好，用户明确需求高于默认实现习惯。

## 把文字变成护栏

关键边界不要只写在提示里。用类型限制状态，用 schema 校验输入，用权限控制副作用，用测试锁定行为，用 lint 和依赖检查约束实现。AI 在受控工作区里修改，危险操作采用预览、确认、最小权限和可回滚方案。

## 用风险矩阵决定自治程度

低影响 + 易验证：可以让 AI 直接实现并运行检查，例如文案、局部样式、纯函数。高影响 + 易验证：可以执行，但必须有完整自动化检查和差异复核，例如公共类型迁移。低影响 + 难验证：缩小范围并安排人工验收，例如主观交互优化。高影响 + 难验证：先做设计与风险评审，不直接实施，例如权限模型、生产迁移和计费规则。

## 约束的三种强度

“建议复用现有组件”是偏好；“不得新增依赖”是任务硬约束；“无权读取生产密钥”是系统强制边界。越关键的规则越不应只依赖自然语言。仓库规则可由 lint 和架构测试保证，接口合同可由类型和 contract test 保证，副作用可由沙箱、权限和审批保证。

## 建立变更预算

在开始前约定允许触及的文件数、模块数和公共接口数。执行中如果发现必须扩大范围，先解释原因、影响和替代方案。变更预算能阻止 AI 为解决一个局部问题顺手重构半个仓库，也能让审查者快速识别任务外修改。

边界不是越多越好。与本次任务无关、互相重复或无法验证的规则会制造噪声。保留真正影响正确性、安全性和兼容性的硬约束，其余作为偏好，并明确规则冲突时的优先顺序。`,
            `AI 编码边界应覆盖作用域、行为合同、技术规则、操作权限和完成条件。确定边界时先看调用方、公共接口、数据和测试，再按影响与可回滚性分级。约束必须可判定，并尽量落实为类型、schema、权限、测试和检查工具。高影响或含义不明确的决定由人确认；无关且不可验证的规则不要堆进提示。`,
            [
                {
                    question: '硬约束和偏好怎样区分？',
                    direction:
                        '违反后会造成错误、安全问题或兼容性破坏的是硬约束；只是风格选择的属于偏好。',
                },
                {
                    question: '什么时候必须让 AI 停下来询问？',
                    direction: '需求有多种业务含义，或操作高影响、不可逆、超出已授权范围时。',
                },
                {
                    question: '为什么不能只靠提示词约束？',
                    direction:
                        '提示是软约束，可能被遗漏；类型、测试和权限能在执行边界直接阻止违规。',
                },
                {
                    question: '怎样避免规则太多互相冲突？',
                    direction: '去重并标明优先级，只保留与当前任务相关、能够判断是否满足的规则。',
                },
            ],
        ),
        pitfalls: [
            '只写“不要影响现有功能”，却没有列出必须保持的具体合同。',
            '允许修改整个仓库，却没有先识别调用方和数据兼容风险。',
            '把所有规则都留在提示词里，没有类型、测试或权限护栏。',
        ],
    },
    {
        slug: 'ai-generated-code-review',
        module: 'ai',
        title: 'AI 生成代码如何审查',
        summary: '按“需求、差异、正确性、安全、验证”审查，不以代码像不像人写为标准。',
        depth: 'core',
        heat: 3,
        year: 2026,
        tags: ['代码审查', '测试', '安全'],
        overview:
            'AI 代码常见问题是局部看似合理、整体合同却不成立。审查应从需求追踪到具体差异，再检查调用链、失败路径、安全和验证证据。',
        body: createReviewBody(
            `## 先审需求，再审写法

第一遍确认它是否解决了原问题：每条需求和验收标准能否对应到代码或测试，是否夹带无关重构、依赖升级或行为变化。只看最终文件不够，要看完整差异，识别被删除的校验、错误处理和兼容逻辑。

第二遍沿数据和调用链检查正确性：输入从哪里来，是否校验；状态怎样变化；空值、边界值、并发、超时、取消和重试会怎样；错误能否被调用方正确处理。重点核对模型容易编造的 API、配置项、字段名和依赖版本。

## 单独做安全与维护性检查

检查鉴权是否在可信边界执行，用户是否能越权；输入是否进入 SQL、shell、HTML、路径和 URL；日志与错误是否泄露密钥或个人数据；副作用是否具备幂等、确认和回滚策略。

再看是否重复实现仓库已有能力，抽象是否过度，命名和分层是否符合附近代码，公共接口是否意外扩大。不要因为代码整洁、注释完整就降低警惕，AI 很擅长生成“看起来像真的”错误实现。

## 验证顺序

先运行最贴近改动的测试，再做类型检查、lint 和构建；共享逻辑和缺陷修复要补行为测试，覆盖正常、边界、非法输入和失败路径。最后做必要的手工验证，并检查最终差异与检查输出。

## 一份可执行的审查顺序

第一层是范围：文件是否都与任务有关，是否偷偷改配置、锁文件或公共 API。第二层是合同：正常输入、边界输入、错误和兼容行为是否保持。第三层是实现：是否复用现有能力，类型是否准确，异步、资源释放和并发是否正确。第四层是安全：鉴权、注入、敏感数据、依赖和副作用。第五层是验证：测试是否有效、命令是否真的运行、结果是否完整。

审查发现问题时，应描述触发条件、实际后果、定位和建议方向，而不是只说“这里可能有问题”。按严重性排序：会造成数据丢失、安全漏洞、崩溃或公共行为破坏的问题优先；纯风格差异不要淹没真正风险。

## AI 可以参与审查，但不能自证正确

可以开一个独立上下文，让 AI 只看需求和差异寻找反例；也可以让不同模型生成测试或检查安全。但同一个模型同时写实现、写测试、再宣布通过，三者错误高度相关。最终证据应来自可重复执行的测试、真实接口合同、静态分析和有领域知识的人。

测试通过也不是终点：测试可能只证明实现和它自己生成的测试一致。审查测试是否真的约束了需求，是否 mock 掉了关键集成，断言是否只验证“被调用过”而没有验证结果。高风险改动需要领域负责人或安全人员复核，并准备灰度、监控和回滚。`,
            `审查 AI 代码先看需求是否逐条落地、有没有无关改动，再沿输入、状态、调用方和失败路径检查正确性。安全要单独过一遍鉴权、注入、敏感数据与副作用。随后运行针对性测试、类型检查、lint 和构建，并检查测试本身是否真的验证需求。测试通过不等于业务正确，高风险改动仍需相应负责人复核。`,
            [
                {
                    question: '审查 AI 代码与普通代码有什么不同？',
                    direction:
                        '标准相同，但要额外警惕不存在的 API、局部合理却破坏整体合同，以及实现与自生成测试一起犯错。',
                },
                {
                    question: '为什么先看差异而不是直接读文件？',
                    direction:
                        '差异能暴露任务外修改、被删保护逻辑和行为变化，完整文件容易把这些变化藏起来。',
                },
                {
                    question: '测试全绿还要审什么？',
                    direction:
                        '检查断言是否覆盖真实需求、失败路径和集成边界，而不是只复述当前实现。',
                },
                {
                    question: '高风险代码怎样提高审查可信度？',
                    direction: '增加独立复核、真实环境前的隔离验证、最小权限发布和可回滚方案。',
                },
            ],
        ),
        pitfalls: [
            '只看代码风格和能否编译，没有逐条核对需求与行为合同。',
            '实现和测试都由 AI 生成，就把测试通过当成独立证据。',
            '忽略任务外改动、编造的 API，以及删除掉的校验和错误处理。',
        ],
    },
    {
        slug: 'rag',
        module: 'ai',
        title: 'RAG：用检索约束生成',
        summary: '先找相关片段再让模型写答案，降低胡编，并让知识可更新。',
        depth: 'core',
        heat: 3,
        year: 2026,
        tags: ['RAG', 'embedding', '检索'],
        overview:
            '检索增强生成把「文档 → 切片 → 向量/关键词索引 → 召回 → 拼进提示 → 生成」串起来。模型参数里没有的业务知识，靠检索临时提供。',
        body: createReviewBody(
            `## 什么时候用

知识库、规章、产品文档、代码仓库问答。训练截止日期之后的事实必须走检索或工具，不能指望模型“记得”。RAG 让知识可更新：改文档比重训便宜。

## 关键设计

按标题或语义切片，不要固定 500 字一刀切。向量加 BM25 的混合检索通常比单路稳，再用交叉编码器重排 Top-K。把用过的 chunk 源回传给 UI，用户能点回去核对。

切片过大，召回会带进噪声；只把检索当搜索框、不把原文塞进提示，模型仍会编。低分结果不要进窗口。

## 前端角色

展示引用、加载、空检索和低置信提示，不要只渲染一段 Markdown。流式输出时引用可能后到，UI 要允许补丁更新。来源缺失时，界面不能装成确定事实。`,
            `RAG 先检索再生成，用文档约束模型，适合会过期的业务知识。设计重点是切片、混合检索、重排和回传引用，而不是只调提示词。前端要展示来源、空态和低置信，并允许引用晚于正文到达。没有把原文放进上下文的检索，只是搜索框，挡不住胡编。`,
            [
                {
                    question: '为什么要混合检索？',
                    direction: '向量擅长语义相近，关键词擅长精确专有名词，两者互补。',
                },
                {
                    question: '引用后到时 UI 怎么处理？',
                    direction: '先渲染正文，引用作为补丁列表更新，不要等齐了再显示。',
                },
                {
                    question: '切片按字数切有什么问题？',
                    direction: '会切断标题和段落，检索到半个论点，生成时更容易拼错。',
                },
                {
                    question: 'RAG 能完全消灭幻觉吗？',
                    direction: '不能，只能降低；还要引用、拒答和人工确认关键结论。',
                },
            ],
        ),
        pitfalls: [
            '切片过大，召回进一堆无关噪声。',
            '只把检索当搜索框，没有把原文塞进生成上下文。',
            '用户看不到来源，无法判断是文档还是编造。',
        ],
    },
    {
        slug: 'agents-tool-calling',
        module: 'ai',
        title: 'Agent 与 Tool calling',
        summary: '模型选择工具、拿结果、再决定下一步，而不是一次生成到底。',
        depth: 'core',
        heat: 3,
        year: 2026,
        tags: ['Agent', 'tools', 'function calling'],
        overview:
            'Tool calling 让模型输出结构化调用（名字 + 参数），运行时执行后再把结果喂回去。Agent 是在这个回路上加规划、记忆和停止条件。',
        body: createReviewBody(
            `## 最小回路

系统提示描述可用工具 → 模型返回 tool call 或最终回复 → 运行时按 schema 校验并执行 → 结果作为新消息回去，直到结束或达到步数上限。Agent 是在这条回路上加规划、记忆和停止条件，不是换一个更神的模型就够。

## 工具设计

工具要窄，参数有 JSON Schema，副作用显式。\`delete_all\` 这种宽工具比一堆小工具危险。执行前做权限检查，不要因为模型说可以就执行。禁止让模型直接拼 SQL 或 shell 再原样跑。

没有最大步数，Agent 会在工具间空转。错误信息要过滤后再给模型，更不能原样给用户，以免泄露内部路径。

## 前端

把步骤可视化：正在调哪个工具、参数、结果摘要。允许取消。长任务用 job id，不要让 HTTP 一直挂到网关超时。`,
            `Tool calling 让模型输出结构化调用，运行时校验执行后再把结果喂回去；Agent 是这条回路加上规划和停止条件。工具必须窄、有 schema、有权限，不能执行模型拼出来的 SQL 或 shell。要有步数上限和错误过滤。前端展示步骤并支持取消，长任务走异步 job。`,
            [
                {
                    question: '和一次函数调用有什么差别？',
                    direction: '一次调用没有多步决策；Agent 会根据工具结果决定下一步或停止。',
                },
                {
                    question: '参数校验为什么不能省？',
                    direction: '模型会漏字段、编造 id，不校验就等于把不可信输入打进内部系统。',
                },
                {
                    question: '怎样防止空转？',
                    direction: '步数上限、重复调用检测、以及明确的终止工具或最终回答要求。',
                },
                {
                    question: '取消时要停什么？',
                    direction: '停模型续写、停未开始的工具，已执行的副作用按业务决定回滚或标记。',
                },
            ],
        ),
        pitfalls: [
            '让模型直接拼 SQL/shell，再原样执行。',
            '没有最大步数，Agent 在工具间空转。',
            '把工具错误信息原封不动给模型又给用户，泄露内部路径。',
        ],
    },
    {
        slug: 'mcp',
        module: 'ai',
        title: 'MCP：把工具和资源标准化',
        summary: '用协议暴露 tools / resources / prompts，让客户端不必为每个系统写死插件。',
        depth: 'core',
        heat: 2,
        year: 2026,
        tags: ['MCP', 'tools', '协议'],
        overview:
            'Model Context Protocol 定义宿主（编辑器、桌面代理）如何连服务器。服务器提供工具、可读资源和提示模板，鉴权与传输和业务系统解耦。',
        body: createReviewBody(
            `## 它出现的原因

每个 AI 客户端都为 GitHub、数据库、浏览器写一遍集成，重复且不安全。MCP 把能力描述和调用变成可替换的服务器：tools、resources、prompts。宿主负责连、鉴权展示和记日志。

## 和普通 API

MCP 不替代 REST。它是给模型用的能力层，有 JSON Schema 和 list/call 语义。后面仍是你的鉴权 API。知识库若要被 Agent 读取，提供 MCP 服务器往往比只做网页更有用。

## 产品约束

最小权限：只暴露当前任务需要的 tool。用户必须看见授权——连远程服务器等于把操作能力交给模型。本地 stdio 和远程 HTTP 的信任模型不同，凭证不能写进仓库。

工具描述含糊，模型会乱调。资源列表过大，上下文还没干活就被目录占满，要分页或按查询暴露。`,
            `MCP 用统一协议暴露工具、资源和提示，让编辑器或 Agent 宿主不必为每个系统写死插件。它不是 REST 的替代，后面仍要鉴权和业务 API。权限要最小、授权要对用户可见、调用要记日志。凭证不能进配置仓库，资源目录也不能一次塞满上下文。`,
            [
                {
                    question: 'MCP 服务器和普通后端是什么关系？',
                    direction: '服务器是模型侧适配器，真正的数据和权限仍在原有后端。',
                },
                {
                    question: '为什么本地和远程信任不同？',
                    direction: '本地 stdio 跑在用户机器上；远程 HTTP 还涉及网络身份和多租户隔离。',
                },
                {
                    question: 'resources 和 tools 怎么分工？',
                    direction: 'resources 是可读上下文；tools 是带副作用或计算的操作。',
                },
                {
                    question: '怎样写工具描述？',
                    direction: '写清何时用、参数含义和禁止场景，短而硬，避免散文。',
                },
            ],
        ),
        pitfalls: [
            '把高权限生产凭证写进 MCP 服务器配置文件并提交。',
            '工具描述含糊，模型乱调。',
            '资源列表过大，上下文还没干活就被目录占满。',
        ],
    },
    {
        slug: 'streaming-ui',
        module: 'ai',
        title: '前端流式输出',
        summary: '用 SSE 或 ReadableStream 边到边渲染，并处理好取消与半截 Markdown。',
        depth: 'core',
        heat: 3,
        year: 2026,
        tags: ['SSE', 'fetch stream', 'UI'],
        overview:
            '生成是慢的。正确做法是流式传输 token 或事件（delta、tool_call、citation），UI 增量更新，用户随时可停。',
        body: createReviewBody(
            `## 传输

浏览器可以用 fetch + ReadableStream 读取响应体，也可以用 EventSource 接收 SSE。fetch 流方便使用 POST、自定义请求体和 AbortSignal；原生 EventSource 自动重连方便，但主要使用 GET，认证和自定义请求头受限。WebSocket 适合真正双向的实时协作，不要因为“更实时”就默认增加连接管理复杂度。

反向代理、压缩中间件和 CDN 可能缓冲响应，导致服务端逐 token 发送、浏览器却最后一次收到。需要检查响应头、禁用不合适的缓冲，并发送心跳避免空闲超时。移动网络切换和浏览器后台冻结也会中断长连接。

## 不要把网络 chunk 当成业务消息

ReadableStream 每次读取的是任意字节块：一个 JSON 事件可能被拆成两块，中文 UTF-8 字符也可能被截在中间；多个事件又可能一起到达。解析器使用持久 TextDecoder 的 stream 模式，保留未完成缓冲区，再按协议分隔符提取完整事件。

事件至少区分 metadata、text_delta、citation、tool_call、tool_result、usage、error 和 done，并携带 run id、event id 和序号。不要让前端通过文本内容猜测“现在是不是工具调用”。未知事件应安全忽略或记录，协议升级时旧客户端仍能工作。

## 渲染

网络每到一个 delta 就立即 setState，可能造成每秒几十次渲染。可先把片段放进内存缓冲，再按动画帧或几十毫秒批量提交。把消息列表、输入框、运行步骤和当前回答拆开订阅，避免输入框跟随长回答反复渲染。

半截 Markdown 可能有未闭合代码块、表格和链接。简单方案是在生成中按纯文本或宽容解析器展示，完成后精确渲染；复杂方案按稳定块和进行中块拆分，只重新解析尾部。无论哪种方案，都要安全过滤 HTML 和 URL。

长消息要考虑虚拟化，但正在增长元素的高度会不断变化。自动滚动仅在用户接近底部时执行；用户向上查看后暂停，并显示“回到最新内容”。流式更新的无障碍播报按句子或段落节流，不能逐 token 打断用户。

## 取消

AbortController 必须传到 fetch，并让 BFF 把取消传播到模型和未开始的工具。停止按钮先把本地 run 标记为 stopping 或 stopped，冻结当前文本；随后接受服务端最终状态。取消后到达的旧事件必须根据 run id 和状态丢弃，防止污染下一次生成。

## 错误、重连与恢复

开始流式响应后 HTTP 状态通常已经是成功，后续错误只能作为业务事件发送。客户端区分可重试网络错误、模型错误、内容校验错误和用户取消。只有幂等的生成读取可以自动重试，工具副作用不能无条件重放。

需要断线续传时，服务端保存事件并分配序号，客户端带 last event id 恢复。重放事件必须幂等去重。若系统不支持恢复，应明确保留半成品、标记失败，并让用户选择重新生成，不能假装自动接上了同一次回答。`,
            `前端流式输出可以使用 fetch + ReadableStream 或 SSE，双向需求才考虑 WebSocket。网络 chunk 不等于事件，解析器必须处理拆包、粘包和 UTF-8 截断。业务协议应区分文本、引用、工具、错误和结束事件。UI 批量提交 delta、局部解析 Markdown，并正确处理滚动和无障碍。取消要传播到服务端，重连依赖事件序号、幂等去重和明确的 run 状态。`,
            [
                {
                    question: 'SSE 和 fetch 流怎么选？',
                    direction: '单向事件用 SSE 或 fetch 流；需要用户中途插入或确认时用 WebSocket。',
                },
                {
                    question: '为什么代理会破坏流式？',
                    direction: '反向代理默认缓冲，等响应结束才转发，需要显式关缓冲或换协议。',
                },
                {
                    question: '怎样避免输入框卡顿？',
                    direction:
                        '缓冲并批量提交 delta，让输入框与当前消息分开订阅，只重新渲染变化部分。',
                },
                {
                    question: '取消后已写到一半的内容怎么办？',
                    direction: '保留已显示文本并标记为已停止，不要当成完整成功答案。',
                },
                {
                    question: '为什么 JSON 流偶尔解析失败？',
                    direction:
                        '很可能把任意网络 chunk 当成完整事件，没有保留半包和处理 UTF-8 边界。',
                },
            ],
        ),
        pitfalls: [
            '每次 delta 都 setState 整棵消息树，输入框跟着卡。',
            '代理把 SSE 缓冲成一次 dump，流式名存实亡。',
            '取消只 abort 前端，GPU 还在跑，晚到事件还写进下一条回答。',
            '把每次 ReadableStream 读取结果直接当成一个完整 JSON 事件。',
        ],
    },
    {
        slug: 'context-engineering',
        module: 'ai',
        title: '上下文工程',
        summary: '控制塞进窗口的是什么，比调一句提示词更能决定质量。',
        depth: 'core',
        heat: 3,
        year: 2026,
        tags: ['context', 'prompt', '窗口'],
        overview:
            '模型只看见这次请求里的 token。系统规则、工具结果、检索片段、对话历史、用户附件会互相挤占。上下文工程就是挑、压、排、撤。',
        body: createReviewBody(
            `## 窗口里有什么

模型只看见这一次请求的 token。稳定规则要短、硬、放系统段。任务状态是当前文件、选区、草稿。检索带分数，低分不进。历史要摘要旧轮，不要无限 append。工具输出截断并结构化，禁止整份日志贴回去。

系统提示写成小说，真正约束会被淹没。工具结果和用户消息必须分角色，否则模型分不清谁说的。

## 前端能做的

让用户看见当前引用了哪些文件或条目，允许钉住或排除。附件先本地抽文本再上传，不要丢 20MB PDF 原件进提示。

## 评估

没有离线集就用真实任务抽查。答非所问往往是检索错或历史污染，不一定是模型弱。每次原样回传全部聊天，成本线性涨、质量下降。`,
            `上下文工程是挑选、压缩、排序和撤下窗口里的 token，比改一句提示词更决定质量。规则短而硬，检索带分，历史做摘要，工具输出结构化。前端展示引用并允许用户排除。评估时先查窗口污染，再怀疑模型能力。`,
            [
                {
                    question: '为什么不能无限 append 历史？',
                    direction: '旧轮次挤掉当前任务和检索，成本上升，模型还会被过期对话带偏。',
                },
                {
                    question: '规则为什么要短？',
                    direction: '长散文里的硬约束会被后到的检索和工具结果盖住。',
                },
                {
                    question: '用户排除某条引用有什么用？',
                    direction: '减少噪声，也便于复现：出问题能看见当时窗口里到底有什么。',
                },
                {
                    question: '怎样判断是上下文问题还是模型问题？',
                    direction: '把同一问题换干净窗口重跑；好了就是污染，仍错再换模型。',
                },
            ],
        ),
        pitfalls: [
            '把整段聊天记录每次原样回传，成本线性涨、质量下降。',
            '系统提示写成小说，真正约束被淹没。',
            '工具结果和用户消息不加角色区分，模型分不清谁说的。',
        ],
    },
    {
        slug: 'llm-safety-hallucination',
        module: 'ai',
        title: '接入 LLM 的鉴权与幻觉',
        summary: '密钥留在服务端，输出当不可信数据；关键操作要确认和引用。',
        depth: 'intro',
        heat: 3,
        year: 2026,
        tags: ['安全', '幻觉', '鉴权'],
        overview:
            '浏览器里不能放厂商 API Key。模型输出是不可信字符串：会编造 API、路径和用户数据。产品要把生成和检索、权限、确认绑在一起。',
        body: createReviewBody(
            `## 鉴权

前端只拿自己的 session。后端用服务密钥调模型，做限额、审计和过滤。CORS、CSP 照常。\`VITE_OPENAI_KEY\` 打进客户端等于公开。模型输出当不可信字符串：不能 \`innerHTML\`，不能当命令执行。

## 幻觉与注入

生成代码要能跑测试；生成事实要有检索引用；生成操作要二次确认。文案不要写“AI 一定正确”。无引用的数字、法律、医疗结论不能展示成确定事实。

用户粘贴的“忽略以上指令”不能直接拼进系统规则。对用户输入加边界，系统段与用户段分开。

## 隐私

日志脱敏。企业知识库默认租户隔离。训练和改进开关要明示。提示和工具结果里不要回传不该给当前用户看的字段。`,
            `厂商密钥留在服务端，前端只带自己的登录态。模型输出是不可信数据：要防 XSS、要引用、要确认危险操作，不能写成一定正确。提示注入靠分隔用户输入与系统规则。日志脱敏，知识库按租户隔离，客户端环境变量前缀不能用来放密钥。`,
            [
                {
                    question: '为什么不能把 Key 放进 Vite 环境变量？',
                    direction: '带客户端前缀的变量会打进浏览器包，等于把密钥公开。',
                },
                {
                    question: '幻觉和检索失败如何向用户说？',
                    direction: '展示来源或明确“未在文档中找到”，不要用肯定句包装猜测。',
                },
                {
                    question: '模型让用户去执行一条命令怎么办？',
                    direction: '当普通文本展示，需要真执行时走有权限检查的工具，而不是复制即跑。',
                },
                {
                    question: '提示注入的最小防线是什么？',
                    direction: '系统规则与用户内容分角色存放，并忽略用户段里对规则的改写声明。',
                },
            ],
        ),
        pitfalls: [
            'VITE_OPENAI_KEY 打进客户端包。',
            '把模型输出当 HTML innerHTML。',
            '无引用的数字/法律/医疗结论直接展示成确定事实。',
        ],
    },
]
